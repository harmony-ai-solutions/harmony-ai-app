# Harmony AI App Active Context
@Focus:
- **Entity ID & tombstone integrity (2026-09-07, COMPLETE — all three repos, UNCOMMITTED):** fixed the delete+recreate incident (new AI stuck "Connecting…" forever — root cause: app purged local tombstones while the engine's own GC was broken, so recreate reused a ghost id → engine `UNIQUE constraint failed: entities.id` → silent rollback → `entity_not_defined`). Now: **ids are derived-only and immutable for life** (`{slug(name)}-{YYYYMMDDHHMMSS}` UTC, D2/D22/D23) minted through ONE app seam `mintEntityId` (D68, `entities.ts`) with reserved-name typed errors (`user`/`deleted`, D33) + ghost-aware `nextFreeDerivedId` backstop; **deletion = tombstone → GC, no restore** (D1 amended by D69–D78; final once propagated + GC'd, recreation is the only path back; 8-child one-`now` cascade D26/D17; settings fully soft D18; `permanent` param removed everywhere by user ruling); **one-time wipe + re-sync from the engine** ships with this update (D11 — boot-window D61, "Rebuilding from Soulbits Engine…" label D58, prefs reset D19, placeholder migrations 000045/000046, force_full_sync escalation); **sync version gate v2** with sticky `serverUpdateRequired` (one `initiateSync` choke point + ~10-min re-probe) + **rebuild-on-purge-floor** (`rebuild_required` → persist wipe flag → `react-native-restart` process restart, D76/D82); sync failures surfaced (typed `SyncConflictError` from `error_code`, critical `syncAndWait`, D55 sync-fully-before-INIT_ENTITY at chat open); failed sessions flagged + error banner + retry (D36/4-3). Gates (6-1 report): engine 43 pkgs 0 fail; app **146 unit suites/1300 + 13 integration suites green**; FE build + node --test green; D11 release-window manual items pending. Plan: `.current_work/entity-id-tombstone-integrity/` (D1–D87 in summary.md); record `17-EntityIdTombstoneIntegrity-Record.md` in `.current_work/senju-rebase-integration/`. **Release order: engine deploys FIRST (D11).**
- **Persona shared module config (2026-09-02, COMPLETE — persona modules 4-1, `senju-design-updates-rebase`):** "One voice, many faces" — personas (user entities) resolve modules from the canonical built-in `user` entity's mapping; identity & memory per-persona, input processing is shared. App delivers a **global "Voice input" user setting** (Settings entry + PersonaEdit info link): the switch maps to the engine provider sentinel (`ProviderDisabled = "disabled"`), and reads are LWW-safe (a *missing* mapping locally means *disabled* — the app never blindly ensure-creates the canonical row, which would win row-LWW and wipe the engine's seeded STT wiring). **Test panels** run over eventserver `debug` sessions + EXISTING events (STT one-shot `STT_INPUT_AUDIO`; TTS `TTS_GENERATE_SPEECH` binary → `ENTITY_UTTERANCE`) — the earlier HTTP module-test client was deleted. Tests execute against the **saved synced config**. Known gaps: live VAD streaming is a documented follow-up; **the TTS test panel is currently runtime-disabled** (no config-editor entry path carries an entity binding — the `entityId` prop is reserved for future wiring). Commits: `3609d3e` (voice input screen), `f10c1ec`/`41ae5d9` (test panels). Gates: tsc 0; unit 120 suites/1016 + integration 10/50 (+1 skip). Plan: `.current_work/persona-shared-modules/` (summary.md + 1-1…4-1).
- **Senju Engine Phase 2 (2026-09-01, COMPLETE — both repos):** Message actions, preferences sync & entity typing. Contract base: `.current_work/senju-rebase-integration/21-Engine-Contract-Persona-Enums.md` (Q1–Q16 + §9 A1–A19). App branch `senju-design-updates-rebase` (commits `1c7d916`, `398bee4`, `40dde10`, `1288630`, `4deb71b`, `156de56`, `6a8788a`, `722e4b5`, `4db2ab7`); engine branch `feat/engine-track-phase2` (commits `b31aad1`, `dccbd29`, `72aed94`, `7f906aa`, `1628357`, `9aca1a1`, `bff4311`, `e032869`, `e726f82`, `bca92f3`, `bd718c9`, `138ea3f`, `982a1a3`, `b68eab2`). **FULL RECORD: `.current_work/senju-rebase-integration/15-Engine-Phase2-Record.md`.** Highlights: canonical `conversation_messages` rebuild + §9-A9 label adoption (byte-identical, never allowlisted); derived unread (`is_read`, entity-scoped A2); synced `reply_mode` + entity-level `is_muted`/`is_disabled`; personas → user entities (engine-seeded "You" default persona); favorites → `character_profiles.is_favorite` (§9-A19, sidecar dropped both sides); parity **55/55 + 3 Go-only allowlisted** (`device_push_tokens`, `sync_devices`, `sync_history`), exit 0. Gates: engine `go build`/`go vet`/`go test ./...` ALL green; app tsc 0, unit 112 suites/961 tests (`nodeSide.test.ts` parallelism flake pre-existing, passes 23/23 in isolation), integration 10 suites/50 pass +1 skip, `entitySessionInitRecovery` green. **NEXT:** on-device smoke list (15-Record), coordinated mainline merge (engine FIRST then app), senju origin force-push window (unchanged), cloud-track lifecycle-worker entity gate.
- **Reply-to-message restore (2026-08-28, COMPLETE, UNCOMMITTED):** keep-but-hidden consensus restored — full reply pipeline re-added by reversing the Phase-4 O4 removal (migration 000041 amended in place + dev-wipe note, model/repo/service wire field, ChatBubble reply header, ChatDetail wiring, action-sheet + input-bar UI). UI gated OFF via `MESSAGE_REPLY_ENABLED = false` in `src/constants/chatFeatures.ts`; flip to enable. Parity: D3 now includes `reply_to_message_id` + `idx_conversation_messages_reply_to` — Phase-2 B1 engine mirror must carry them. Gates green: tsc 0, unit 98/873, integration 10/50+1 skip.
- **Senju Follow-Up Phase 1 (2026-08-25, COMPLETE):** App-side stub/parity/streamlining phase on `senju-design-updates-rebase` (docs: `.current_work/senju-followup-phase1/` 1–9 + logbook `.current_work/senju-rebase-integration/13-Phase-1-Logbook.md` + record 14). Stub service layer (MarketplaceService/WalletService/SocialService/NotificationService, O5 all-builds, honest errors, upload-copy publish A4, `StubServiceError` ≡ client `APIError` surface); ALL consumers rewired (Market/Discover/AIProfile/MyListings/MyLibrary/ContentAsset/posts/notifications/blocked/users/profile screens + `CharacterCardImportService`); client-side paywall REMOVED (chat opens freely); `UserProfileStore` + 6 sidecar repos + `SoulbitsDefaultConfigService` deleted; migrations 41–55 consolidated into ONE `000041` (personas + character_favorites + chat_conversation_settings + message actions minus reply — 15 migration files deleted); CLIENT_ONLY_TABLES interim = 3 (`personas` dies B3, `character_favorites`+`chat_conversation_settings` redesigned B2); parity = expected narrowed-D3 end state (RN-only `idx_conversation_messages_pinned`, Go-only `device_push_tokens`, diff-SQL 11 = conversation_messages + 10 cosmetic); B4 seeding reverted (module pickers: "Disabled" option back, no auto-defaulting); chat-list fixes F1/F3/F5/F6/F7/F8/F10/F11/F12 + reply-mode toggle (A6) + recording kept on 120s auto-stop (D1-3) + Kotlin `show()` → Promise<boolean> (D1-4, device-build compile pending) + 3-state connection indicator + first-message day divider + stats aggregate (D1-5/7/8/12); INIT_ENTITY recovery live (`entitySessionInitRecovery.test.ts` spec re-enabled 4/4 green UNMODIFIED, bounded 2 retries); editor consolidated into CreateAI edit mode (V3 sections, image-churn fixed via `imageReconcile`, `CharacterProfileEditScreen` DELETED, Q-D4a closed). Gates: tsc 0; unit 95 suites/834 tests (12 snapshots); integration 10 suites/50 pass +1 skip (pre-existing skip — NOT entitySessionInitRecovery); parity expected set exactly; gitnexus analyze 7.039 nodes/14.268 edges/189 clusters/300 flows; grep sweeps zero in src/. Sibling repos untouched (harmony-link-private read-only for parity). OUTSTANDING: on-device smoke list (Market/Discover/Chat create→chat reliability/Editor round-trip); senju origin force-push coordination window; engine Phase 2 planning round (B1 Go mirror closes D3, B2 favorites/settings redesign, B3 personas→entities → `21-Engine-Contract-Persona-Enums.md`).
- **Senju Rebase Integration (2026-08-24, COMPLETE):** All 52 `senju-design-updates` commits (senjuLawliet) replayed verbatim onto `feat/cloud-lifecycle` (fbab5ce) → branch **`senju-design-updates-rebase`** (pushed; tip 8bc09b3). Full record: `.current_work/senju-rebase-integration/` — 01 playbook, 10/11/12 execution records, 02 followup plan (Tracks A–E), summary.md decision register (D1–D6, R7/R8, O-rulings). HEADLINES: migrations 1–55 final (ours 35–40 engine-mirror UNshifted — 000039 reserved placeholder; hers renumbered +6 → 41–55); her code ported to V3 columns + UUID image ids (000047 image_id→TEXT); D4 editor suite kept comparison-only (`CharacterProfileEdit` route registered, unlinked); `entitySessionInitRecovery.test.ts.skip` = executable spec for Track E. Gates green: tsc 0 errors, unit 84/84 suites · 794 tests, integration 10/10; schema parity = D3 (`conversation_messages` + its 2 indexes) + 10 pre-existing cosmetic drifts + `device_push_tokens` Go-only. OUTSTANDING: (1) coordinated force-push of HER `senju-design-updates` origin branch — local already at replayed tip 53ba29a, origin still 5204fb5; do NOT push without the agreed window; (2) followup Phase 1 = app-side B5 sidecar-table drop pass + Track A stubs BEFORE any engine work (engine track LAST, own planning round). Cross-verified 2026-08-24 vs sibling repos: engine migrations 35–40 mirror ✓; engine lacks message-action + read-flag columns (D3/B1 premise) ✓; engine `DeriveParticipantKey(participantIDs, entityID, scope)` includes own entity (O3) ✓; engine seeds "Default SoulbitsCloud" rows (B4 literal) ✓; api-client wire shapes (isQuotaError/soulCreditsAvailable/currentTier/upgradeUrl/subscription) ✓; backend `PATCH /v1/auth/me` EXISTS but display_name-only → A4 amended to "extend, don't redesign" (its e2e "does not exist" comments were stale, fixed).
- **Device Authorization Button Flow (2026-08-15):** Phases 4 + 5 of the soulbits-cloud-backend device-auth-button-flow workstream. (1) `soulbits://` CUSTOM SCHEME — `soulbits://device-auth?code=<6-digit>` deep link handled in new `src/services/cloud/deviceDeepLink.ts`; per design it fires ONLY from the portal page in a browser, never from the email body (immune to mail clients stripping custom schemes; no assetlinks/apple-app-site-association infra). (2) CLIENT PIN BUMP — `@harmony-ai-solutions/soulbits-api-client` re-pinned to the pushed commit `cb85ec9` (git+ssh GitHub dependency, `prepare` builds tsup dist). (3) MODAL POLLING + AUTO-CONTINUE — `DeviceAuthModal` polls `GET /v1/devices/authorize/status` every 5 s while visible; when `authorized` flips true it closes itself and retries the cloud connect; the manual 6-digit code entry stays as fallback. i18n copy in `auth.json` + tests (`DeviceAuthModal.render.test.tsx`, `deviceDeepLink.test.ts`, `deviceAuth.test.ts`). All UNCOMMITTED with the rest of the phase stack. Verification: `npx tsc --noEmit` 0 errors; jest unit 71 suites / 611 tests green.
- **Character Profile Editor — Desktop-UI Parity Alignment (2026-08-11):** Brought the RN character profile editor to parity with the Wails frontend's recent layout/i18n changes (the 7-bullet editor feedback + lifecycle editor port). (1) SECTION REORDER in `src/screens/CharacterProfileEditScreen.tsx`: Identity → Voice & Persona → Images (now 2nd, was last) → Greeting → Scenario → Lorebook → Lifecycle (NEW) → Advanced → Attribution (now last). (2) LOREBOOK ENTRY EDITOR (`src/components/character-card/LorebookEntryEditor.tsx`): removed the Advanced (export-only spec fields: position, insertion_order, priority, selective/secondary keys, case_sensitive, use_regex, id) section from the UI; reordered to Name/Comment → Content → Keys → Matching (enabled + constant). Advanced fields carried verbatim from the seeded draft on save for card round-trip parity with Wails (removed now-unused state: advancedOpen, secondaryKeysText, position, insertionOrderText, priorityText, idText + POSITIONS const). (3) NEW RN `src/components/character-card/LifecycleConfigEditor.tsx` — full port of the Wails editor: 5 sections (Beat Schedule, Sleep & Exhaustion, Emotion Decay, Crystallization, Memory) with autonomy chips + numeric TextInputs + per-field detailed hints. Mobile-native collapsible section info: each section header has a ⓘ icon; tap → expand a bordered description card inline (no portal needed — it expands within the scroll, sidestepping the Wails overflow:hidden + backdrop-filter clipping issue entirely). Exports `LifecycleConfig` interface + `LifecycleConfigEditor` component. (4) i18n `src/i18n/locales/en/characters.json`: `advanced` "Advanced" → "AI Behaviour"; new `lifecycleSection` = "AI Lifecycle"; + full nested `lifecycle.*` block (52 keys, verbatim from Wails — defaultsNote, 5 section groups with title/tooltip + per-field label/unit/hint, autonomyLevels enum, beatTypes map). (5) LIFECYCLE WIRING in the screen: new `lifecycleConfig` state (`useState<LifecycleConfig>({})`); `loadProfile` parses `profile.lifecycle_config` JSON via `parseJsonColumn<LifecycleConfig>`; `buildProfileFields` now includes `lifecycle_config: JSON.stringify(lifecycleConfig)`; removed the create call's hardcoded `lifecycle_config: '{}'` and the export's (now flows through buildProfileFields); defaults banner (accent-tinted card, info icon + `lifecycle.defaultsNote`) above the editor. VERIFICATION: `npx tsc --noEmit` exit 0; JSON valid; 48 static lifecycle literals resolve, 0 missing (4 dynamic `${type}`/`${lvl}` template keys excluded from the literal check); section order confirmed via grep (10 sections in target order). SENJU BRANCH CLASH CHECK (`senju-design-updates`, 18 commits — personas + Discover/Market refactor): touches `CharacterProfileEditScreen.tsx` ONLY in imports + the create-on-save path (+3 lines, `setCharacterProfileSource(newId,'user')`) — non-overlapping with our JSX/state/section work; does NOT touch `LorebookEntryEditor.tsx` or `characters.json`; heavily rewrites `characters.ts` (+170 — AVOIDED; lifecycle persisted via existing full-object `updateCharacterProfile` spread, no repo edit). No migration needed (lifecycle_config column already exists). tsc clean. Changelog updated. NOT yet device-verified.
- **Characters Screen — Favorites + Custom Categories (2026-08-12):** Per user request, the My AI Characters screen now has a filter system so users can set favorites aside and organize characters into categories they choose. DATA: three new CLIENT-ONLY tables via migration 000039 (`character_categories`, `character_category_members`, `character_favorites`) — never synced to the engine (strict schema parity; `CLIENT_ONLY_TABLES` in scripts/dump-schema.ts extended). Repo fns in `src/database/repositories/characters.ts`: favorites (isCharacterFavorite/addCharacterFavorite/removeCharacterFavorite/toggleCharacterFavorite/getFavoriteCharacterProfileIds) + categories (getCharacterCategories/createCharacterCategory/renameCharacterCategory/deleteCharacterCategory/getCharacterCategoryMembers/addCharacterToCategory/removeCharacterFromCategory/getCharacterProfileCategories). UI on `CharactersScreen`: horizontal chip row under the search bar — **All** | **Favorites** | each user category | a **"Manage categories"** chip (plus-box icon + label, dashed border); `activeFilter` ('all' | 'favorites' | category id) drives filteredProfiles (combined with name/description search). Cards (`CharacterProfileCard`) gained a clean heart toggle only (bottom-left, filled accent when favorited; icon-only with text-shadow — NO circle backdrop). LONG-PRESS context menu (`CharacterCardMenuModal.tsx`): a card long-press opens a bottom sheet with **Add to category** (opens `AddToCategoryModal.tsx` checkbox picker) and **Delete** (keeps the existing confirm dialog). Category MEMBERSHIP also lives in `ManageCategoriesModal.tsx` (create/rename/delete categories + expandable per-category member list with a **plus** button per category to toggle which characters belong, immediate persist) — the separate CategoryAssignModal was REMOVED per user feedback (they disliked the shape button on the card). SAFE-AREA FIX: the manage-categories sheet now applies `paddingBottom: safeBottom + 24` (via useSafeAreaInsets) so the category-name input is no longer hidden under Android navigation buttons. Empty states differentiate no-profiles / no-search-results / no-favorites / empty-category. Tab bar label: Characters tab is now **Characters** on the nav bar only (MainTabNavigator tabBarLabel + GlassTabBar FALLBACK_LABELS; header title stays "My AI Characters"). i18n keys added to characters.json (cardMenuAddToCategory/cardMenuDelete/addToCategoryTitle/addToCategoryNoCategories). Tests: +13 in characters.test.ts (favorites 5 + categories 7 + default). tsc clean; unit suites pass except the 2 documented pre-existing better-sqlite3 cross-worker contamination files (pass in isolation); eslint clean on new code (only pre-existing warnings). NOT device-verified yet.
- **"Block AI" Rebranded to "Disable AI" (2026-08-20, user request):** Per user request ("delete and remove completely the block AI feature and replace it with a disable instead") the entire conversation "Block AI" feature was renamed to **Disable AI** while keeping identical behavior (a disabled AI can neither send nor receive messages until re-enabled). SCOPE: targets ONLY the per-conversation block in `chat_conversation_settings` — NOT the separate "Blocked Users" (cloud-user) feature, which is untouched. CODE RENAME: repo `chatConversationSettings.ts` → `ChatConversationSettings.disabled` field + `setConversationDisabled`/`getDisabledConversation`/`getDisabledEntityIds` (the physical `blocked` DB column is kept for migration safety); `EntitySessionService` → `isSessionDisabled`/`setDisabledOverride`/`assertNotDisabled`/`disabledOverrides`; UI handlers `handleToggleDisable`/`handleDisableToggle`; `ChatConversationMenuModal` props `isDisabled`/`onToggleDisable`. UI: `BlockedAIsScreen.tsx` DELETED → new `DisabledAIsScreen.tsx` (route `DisabledAIs`; `AccountSettingsScreen` navigates there; nav param doc updated). i18n: `settings.json` (`disabledAIsTitle`/`disabledAIsEmpty`/`disabledAIsEmptyHint`/`disabledAIsEnable`/`disabledAIsEnabledToast`), `chatList.json` (`menuDisable`/`menuEnable`/`toastDisabled`/`toastEnabled`/`disableConversationBody`), `chatDetail.json` (`disableTitle`/`disableBody`/`disabledBanner`/`enable`/`toastDisabled`/`toastEnabled`). Migrations 000044 + repository storage column unchanged for safety; storage flag `'blocked'` in `listConversationsByFlag`/`getDisabledEntityIds` retained deliberately. Tests: `chatConversationSettings.test.ts` updated (10/10 green); `tsc --noEmit` clean. Changelog updated (Chat & Messaging > Changed). UNCOMMITTED.
- **Block Users Feature (2026-08-20, user request):** Added the ability to block cloud users from their public profile. On `UserProfileScreen`, a **⋯** menu now sits next to the Follow button (`user-profile-more-button`) opening a dropdown with **Block/Unblock**; blocking shows a themed confirm dialog (`useAppAlert`, destructive action) and unblocking is instant. Repository: the scaffolded-but-missing `blocked_users` functions were implemented in `userSocial.ts` — `getBlockedUserIds`, `isUserBlocked`, `addBlockedUser` (also drops any existing follow), `getBlockedUsers` (`BlockedUserEntry`), `removeBlockedUser`; all added to the default export. Migration **000047** (`blocked_users` client-only table) was registered in `migrations.ts` (the file existed but was NOT wired in) and added to `CLIENT_ONLY_TABLES` in `scripts/dump-schema.ts`. Filtering wired into every surface via `blockedContent.ts` helpers (`filterBlockedCharacterProfiles`/`filterBlockedUserPosts`/`filterBlockedUserNotifications`): Discover (profiles + posts), Market (listings), Characters (all profiles), Notifications (feed), UserProfile posts. Blocking hides a user's AIs, marketplace listings, posts + notifications app-wide; unblocking restores them immediately. Settings: new `BlockedUsers` route (`BlockedUsersScreen` already existed) registered in `AppNavigator` + reachable from **Settings → Account & Security** (`AccountSettingsScreen` new row, `blockedUsersTitle` settings key). i18n added to `profile.json` (`moreActions`/`blockUser`/`unblockUser`/`blockUserConfirm*`/`cancel`/`userBlockedToast`/`userUnblockedToast`/`blockedUsers*`) + `settings.json` (`blockedUsersTitle`). Migration snapshots regenerated. Tests: `blockedContent.test.ts` 6/6, `userSocial.test.ts` 9/9 green; tsc + eslint clean. Changelog updated (Security & Privacy). UNCOMMITTED.
- **Module Config Selectors — No "Disabled" Option, Always Soulbits Cloud Defaults (2026-08-18, user request):** Per user request ("see modules config in the screen of creating AI partner or editing their settings, it must not show disabled at all, it should always show the soulbits defaults config unless the user changes them"), the AI module config selectors on the Create AI Partner / Edit AI Settings screen (`CreateAIScreen`, used for create + edit + duplicate via `editProfileId`/`duplicateProfileId`) no longer offer a **Disabled** option, and every empty module slot is auto-selected with the idempotent **Soulbits Cloud (default)** config. CHANGES: (1) `EntityModuleSelector.tsx` — removed the hardcoded `{ id: -1, name: 'Disabled', value: '' }` option from the sheet and the `?? 'Disabled'` fallback label (now `?? 'Select config'`); the '' (no-config) state is only ever transient while defaults are resolving. (2) `CreateAIScreen.tsx` — new `configSelectionsRef` (live mirror of the 8 module selections so the async filler never overwrites an edit/duplicate mapping applied mid-flight) + a new `useEffect` that runs once `hasAnyConfigs !== null`, calls the already-idempotent `ensureSoulbitsDefaultConfigs()` (reuses existing rows; `getXConfigByName(DEFAULT_CONFIG_NAME)`), and sets each slot whose CURRENT selection is '' to the matching default id, merging the default option into the config list via `mergeDefaultOption` (`DEFAULT_CONFIG_NAME` imported from `SoulbitsDefaultConfigService`). Dependency array includes the 8 selection states so unset slots of a duplicated/edited partner (mapping applied by the prefill effect, which sets unset slots to '') get filled in after the prefill settles. The old save-time fallback (`if !anySelected → ensureSoulbitsDefaultConfigs()`) is preserved as a safety net but is now redundant (the screen never leaves a slot empty). tsc clean; eslint 0 errors (5 pre-existing warnings unrelated). UNCOMMITTED.
- **Marketplace Chat HARD GATE (2026-08-16 follow-up):** Per user request ("make it impossible to open the chat screen with the AI that is not free"), chatting with a marketplace-listed AI the user has NOT purchased (and does not own) is now IMPOSSIBLE until payment lands: the Chat tap is **silently ignored** (no dialog, no navigation, no toast). Implemented in `MarketplacePurchaseService.isChatLocked(profileId, userId)` (= `!canChatWithCharacter`), checked at the screen level (AIProfile `handleChat`, Discover `handleChat`, Market `handleChat`) AND defensively inside the shared chokepoint `CharacterChatService.openCharacterChat` (now takes optional `currentUserId`, silently returns when locked) so ChatDetail can never be reached from ANY entry point — incl. Characters card chat + ChatList "new chat" picker (both now pass `user?.id`; ChatListScreen gained the `useAuth` hook). The old `confirmPurchaseIfNeeded` branded buy dialog is preserved (unused) for when payment is implemented. ALSO: replaced the two wrong `diamond-stone` SOUL icons with the brand `SoulIcon` monogram (CreateAI price field + CharacterProfileCard price pill). tsc clean. UNCOMMITTED.
- **Private-by-Default AI Characters + Marketplace (SOUL) Selling (2026-08-16):** Per user request, newly created AI characters are now **private by default** (only the creator sees/chats). The Create AI Partner screen's **Details** section gained a 3-way **Sharing & Visibility** selector (Private / Public / Marketplace) replacing the old Public/Private switch in General. **Public** = visible + searchable on Discover (view profile + free chat). **Marketplace** = listed on the Market screen with a **SOUL price**; the profile is freely viewable but **chatting requires a one-time purchase in Souls** (creators set the price; settings remain creator-only — other users only ever see the view+chat profile, never the config). DATA: two new client-only migrations — **000045** (`character_marketplace_listings` profile_id PK + `price_souls` REAL, `soul_wallet` single-row balance id=1, `soul_purchases` id/profile_id/price/purchased_at, all FK-cascade on profile delete) and **000046** (rebuilds `character_profile_sources` with the CHECK extended to `('public','private','marketplace')` via the `_new`-table pattern). Repo: new `src/database/repositories/marketplace.ts` (listings upsert/remove/query JOINed with visibility, `getSoulBalance`/`creditSouls`/`debitSouls`, `purchaseCharacter` guards not-listed/already-purchased/insufficient, `canChatWithCharacter` owner-or-purchased-or-not-paywalled) + 18 unit tests. `getPublicCharacterProfiles` now ALSO returns `marketplace` items (they stay visible on Discover). UI: `MarketScreen` rewritten from "Coming Soon" to a live 2-column listing grid (MarketListingCard w/ price badge + chat button, real Soul balance badge, search + refresh); `CreateAIScreen` persists visibility + listing price on create/edit, defaults private, loads price on edit/duplicate; `AIProfileScreen` shows a lock + "Chat · {{price}} Souls" button for listed items and the chat handler routes through the shared `MarketplacePurchaseService.confirmPurchaseIfNeeded` (branded buy dialog → `purchaseCharacter` → toast → open chat); `DiscoverScreen`/`CharacterProfileCard` show a price pill for marketplace items and route chat through the same gate. NOTE: Soul balance is CLIENT-LOCAL (no backend wallet yet) — the Market balance badge reads `soul_wallet`; creator earnings are credited via `creditSouls` when the seller UI is added. UNCOMMITTED.
- **Messenger-Style Multi-Bubble Stack (2026-08-15):** Per user request, the floating chat bubble was upgraded from a SINGLE bubble to a Messenger-style STACK of up to **4 bubbles** (one per conversation), all glued together and dragged as one unit. This also FIXES the reported stale-avatar bug (opening bubble B after bubble A kept A's avatar): the root cause was `ChatBubbleService.showBubble()`'s `if (bubbleView != null) return` early-return — the avatar was only ever painted on the FIRST show, so a second show() with a new conversation updated `conversationJson` (hence the floating window showing B's conversation) but never repainted the avatar ImageView. FIX + FEATURE (native, `android/app/src/main/java/ai/soulbits/chat/ChatBubbleService.kt`): rewrote the single-bubble state into `LinkedHashMap<String, BubbleEntry>` keyed by `participantKey` (insertion order = stack order top→bottom). Each `BubbleEntry` holds its own `view`, `params`, `badgeView`, `unreadCount`, `conversationJson`. A shared `stackX`/`stackY` positions the stack; `updateAllBubblePositions()` fans bubbles out vertically (`BUBBLE_STEP_DP=8`). Gesture handling in `handleBubbleTouch(key,...)` — dragging ANY bubble translates the whole stack; tap opens THAT bubble's conversation (`snapBubbleToMiddleRightThenOpen(key)` → `openFloatingChat(entry)`); tapping a different bubble while a window is open calls `switchOpenConversation(key)` (tear-down + reopen); long-press + drag to ✕ dismisses only that bubble (`animateDismiss(key)` → `removeBubbleEntry(key)`). New actions: `ACTION_HIDE_ONE` (remove one bubble via `EXTRA_PARTICIPANT_KEY`) and `setUnreadCount(key, count)` per-bubble badge. Full-stack eviction: when `bubbles.size >= MAX_BUBBLES` (4) and a NEW conversation arrives, the OLDEST bubble (`bubbles.entries.first()`) is replaced automatically (per user choice). Reopening an existing conversation repaints its avatar + refreshes its payload (the actual bug fix). `openInApp()`/`emitOpenEvent()`/notification now resolve the ACTIVE bubble's conversation instead of the single `conversationJson`. `bringBubbleToFront()` re-asserts ALL bubbles above the chat window. JS (`src/services/ChatBubbleService.ts`): `activeBubbleConversations` Map + `getActiveBubbleConversations()` (all, stack order), `getActiveBubbleConversation()` (legacy, most recent), `hideBubble(key?)` (one or all via native `hideOne`), `setBubbleUnreadCount(count, key?)` (forwards `participantKey`). `ChatListScreen.syncBubbleUnreadBadge` now loops over ALL active bubbles. `ChatBubbleModule.kt` gained `hideOne(key)` + `setUnreadCount(count, key)`. New unit suite `src/services/__tests__/ChatBubbleService.test.ts` (8 tests). tsc clean; unit 52 suites / 538 tests green. Changelog updated (Chat Actions > Added). UNCOMMITTED — requires Android rebuild to verify on device.
- **Floating Chat Window Background-Open Load Fix (2026-08-15):** User reported the floating chat window (opened by tapping the bubble over another app while the app is backgrounded) stayed on the loading spinner forever, but loaded normally when opened while inside the app. ROOT CAUSE (native, RN 0.86 new architecture): the floating window is a ReactSurface on the SAME ReactHost as the main activity. When the activity backgrounds, RN's ReactHostImpl.onHostPause() → ReactContext.onHostPause() → JavaTimerManager.onHostPause() sets isPaused=true and removes the Choreographer frame callback — ALL JS timers (setTimeout/setInterval) freeze. The overlay's FloatingChatContent waits for DB readiness via a setInterval poll + the theme + message queries; with timers frozen these async steps never complete, so the spinner never resolves. When the user returns to the app, onHostResume() un-freezes timers and everything loads instantly (matches the reported behavior exactly). FIX (android/app/src/main/java/ai/soulbits/chat/ChatBubbleService.kt): openFloatingChat() now calls forceResumeHostForOverlay() BEFORE surface.start() — it checks host.lifecycleState != RESUMED and calls host.onHostResume(lastResumedActivity) (1-arg overload, avoids clobbering ReactActivity's DefaultHardwareBackBtnHandler). This un-freezes the JS timer pipeline so the overlay loads even while the activity stays backgrounded. closeFloatingChat() calls restoreHostPauseAfterOverlay() which calls host.onHostPause() (no-arg — no activity-identity assertion) ONLY if we actually force-resumed AND the app activity is NOT foreground (tracked via isAppActivityResumed, set in onActivityResumed/onActivityPaused + read from host.lifecycleState at service onCreate). The onActivityResumed callback clears hostResumedForOverlay so a window closed after the user returned to the app never pauses a foregrounded host. Side-effect analysis: force-resume emits appStateDidChange:'active' to JS — harmless (EntitySessionService only acts on 'background'; BiometricLock only cancels deferred locks on 'active'; ChatBubbleService JS closes window on 'background' only). Double-pause is idempotent (ReactLifecycleStateManager BEFORE_RESUME branch is a no-op). No JS changes needed. CHANGELOG updated (Chat & Messaging > Fixed). Native-only fix — requires an Android rebuild to verify. UNCOMMITTED.
- **Floating Bubble Permission-Flow Hardening (2026-08-14 bug fix):** User reported "Overlay permission is required to show the chat bubble" even after enabling display-over-other-apps. Root cause in src/services/ChatBubbleService.ts: the post-return handler ran a SINGLE hasPermission() check 400ms after the user returned from the OS overlay-permission settings; many devices apply the toggle asynchronously, so the check missed a correctly-granted permission and resolved requestBubblePermission() false, surfacing the misleading bubblePermissionDenied toast. FIX: subscribeToPermissionReturn now starts a fast grace poll (PERMISSION_RETURN_POLL_INTERVAL_MS=300 up to PERMISSION_RETURN_GRACE_MS=10000) that keeps re-checking after the user returns and auto-shows the pending conversation the moment the grant is observed, only settling false if the whole grace window elapses. Also decoupled transient showBubble failures from the permission verdict (recheckOverlayPermission no longer resolves the request from a failed show — it logs and lets the poll retry), and showBubble now logs when native show() returns false despite permission being granted. CHANGELOG updated (Chat Actions > Fixed). tsc clean. UNCOMMITTED.
- **Chat List Conversation Actions + Floating Bubble + Block System (2026-08-14):** Per user request, the Chat list gained the full messaging-app long-press menu: **Pin/Unpin**, **Archive/Unarchive** (dedicated `ArchivedChatsScreen` route — archived chats moved OUT of the inline collapsible into a separate screen with an "Archived (N)" toggle in the Chat list header), **Mute/Unmute**, **Open chat bubble**, **Mark as read/unread** (label now purely driven by `unreadCount > 0`), **Block/Unblock**, and **Delete** (soft-deletes the interaction + its messages via new `deleteConversationByParticipantKey`). Persistence: NEW CLIENT-ONLY table `chat_conversation_settings` (migration **000044**: `participant_key` PK, `entity_id`, `pinned`/`archived`/`muted`/`blocked` 0/1, `unread_count`) + repo `src/database/repositories/chatConversationSettings.ts` (10 fns, 12 unit tests). Unread counts increment in `EntitySessionService.handleIncomingMessage` only when the conversation is NOT registered open (`registerOpenConversation`/`unregisterOpenConversation` on ChatDetail mount/unmount). BLOCK semantics: blocked conversations STAY in the list, show a shield icon next to the time badge (all status icons pinned/muted/blocked sit in a right-aligned `statusColumn` next to the time), and the ChatDetail input bar is fully REPLACED by a red "You blocked this AI" banner (with `safeBottom` padding so it isn't under the nav buttons). Enforcement: `EntitySessionService.isSessionBlocked` (via participant key) guards BOTH outbound sends (`assertNotBlocked` in sendText/audio/image/combined) AND incoming (`handleIncomingMessage` drops + no unread). CRITICAL unblock-latency fix: new **in-memory `blockedOverrides` map** + `setBlockedOverride(participantKey, bool)` — the UI (ChatDetail/chat-list menu/Archived/BlockedAIs screens) calls it BEFORE the DB write resolves so the next send works instantly (was: stale DB read → "AI blocked" error for a while after unblocking). **Floating chat bubble**: native Android `ChatBubbleService` (foreground service, `TYPE_APPLICATION_OVERLAY`, draggable avatar bubble with `chat_bubble.xml` layout, tap → launch MainActivity + emit `ChatBubble.open` via DeviceEventEmitter), `ChatBubbleModule`/`ChatBubblePackage` (registered in `MainApplication`; manifest gained `SYSTEM_ALERT_WINDOW` + `FOREGROUND_SERVICE_SPECIAL_USE` + `<service foregroundServiceType="specialUse">`). JS wrapper `src/services/ChatBubbleService.ts`: `showBubble` remembers the conversation when permission is missing, `requestBubblePermission` races THREE paths (native promise, AppState return, 3s poll up to 60s) — the unified `resolvePermission(true)` ALWAYS auto-shows the pending conversation (fixes "overlay permission required" even after granting). AppShell subscribes `onBubbleOpen` → navigates to ChatDetail with the handoff payload (carries `ownEntityId` so ChatDetail's `entityId` param = own/impersonated entity). **Blocked AIs screen** (`BlockedAIsScreen` route) under Settings → Account replacing the ComingSoon row — avatar + name + one-tap Unblock (with override). i18n keys added to chatList/chatDetail/settings. UNCOMMITTED. tsc clean; unit 51 suites / 530 tests green (the intermittent `cross-repo` FK test passes in isolation — pre-existing better-sqlite3 worker contamination, not this work).
- **Unified Themed Toasts — Last Native ToastAndroid Removed (2026-08-14):** Per user request ("update all the toasts of android to match the app design"), the LAST remaining OS-native Android toast was eliminated. Audit found all screens (ChatDetail, Characters, Discover, MyProfile, AIProfile, ChatInputBar) already use the branded `useToast()` glass-pill toast, and modals use `useInModalToast`/`useTopToast`. The only native caller was `SyncConnectionContext.showToast`, which did `ToastAndroid.show(...)` on Android / `Alert.alert(...)` on iOS. FIX: `SyncConnectionContext` now consumes the themed `useToast()` (AppToastProvider is mounted ABOVE SyncConnectionProvider in App.tsx) through a `showToastRef` mirror — identical pattern to the existing `showAlertRef` — and the local `showToast(message)` just forwards to `showToastRef.current(message)`. Removed the `ToastAndroid`/`Platform`/`Alert` imports. Result: every toast in the app on every platform is now the themed obsidian-glass pill. Verified: tsc --noEmit clean; sync connection unit suites 3 suites / 63 tests green. systemPatterns.toon updated (provider nesting note now covers AppToastProvider too). UNCOMMITTED.
- **Public/Private AI Toggle + Discover Visibility + Delete Cascade Fix (2026-08-14):** Create AI Partner wizard gained a **Public AI / Private AI** toggle in the General section (glass card, earth/lock icon, `Switch`); `isPublic` state default true, persisted to the CLIENT-ONLY `character_profile_sources` sidecar via migration 000043 (`visibility TEXT NOT NULL DEFAULT 'public' CHECK (visibility IN ('public','private'))`). New repo fns: `setCharacterProfileVisibility` / `getCharacterProfileVisibility` / `getPublicCharacterProfiles`; `getCommunityCharacterProfiles` now also filters public-only. **Discover** loads `getPublicCharacterProfiles()` (own public partners show; private hidden from grid + search); Discover card tap opens **AIProfile** (not edit) per user feedback. Toggle persists on create + edit; loaded in edit + duplicate prefill. i18n: `visibilityLabel`/`visibilityPublic`/`visibilityPublicHint`/`visibilityPrivate`/`visibilityPrivateHint` (createAI.json); styles `visibilityRow`/`visibilityIconWrap`/`visibilityLabelGroup`; testID `create-ai-visibility-toggle`. **BONUS — delete AI char didn't work:** `deleteCharacterProfile` soft-delete throws "in use" for every AI char (linked entity). New `deleteCharacterProfileCascade(profileId)` soft-deletes linked entities first (via `deleteEntity` cascade) then the profile; CharactersScreen delete uses it + shows `deletedToast`. Repo tests +5 (visibility 4 + cascade 1; 42 total); migration snapshots regenerated; tsc + jest green; changelog updated. UNCOMMITTED with prior WIP.
- **AI Profile Social Layer (2026-08-13):** Per user request, the AI character profile (`src/screens/AIProfileScreen.tsx`) gained a full social layer. **Chat** is now the primary button (opens a direct conversation via the new shared `src/services/CharacterChatService.ts` — persona resolution + entity reuse/create + pre-chat sync, same logic the Characters screen card chat button uses). Two small rounded buttons beside it: **Like** (heart — counts toward the profile's Likes stat) and **Save** (bookmark — saves the character to My Profile > Saved). **Creator-only controls:** the **Edit Profile** + **Edit AI Settings** pills appear ONLY for the recorded creator of the character (drives to `CharacterProfileEdit` / `EntityConfigEdit`), determined by a new client-only `character_creators` sidecar + `isCharacterCreator` (creator is recorded on Create AI / profile-edit create / character-card import via `setCharacterCreator`, first-writer-wins). **Creator badge** — a small pill with the creator's avatar + name sits under the AI name and navigates to My Profile on tap. **Images tab as posts:** every gallery image (incl. the avatar) renders as a post card with a like button (client-only `character_image_likes`) and a comment button opening `ImageCommentModal` (bottom sheet with `character_image_comments` list + composer, author avatar/name, author-only delete). **My Profile Saved tab** (`MyProfileScreen`) now lists saved AI characters as avatar + name tiles that open the AI profile (`character_saves` + `getSavedCharacterEntries`). New migration **000041** creates 5 CLIENT-ONLY tables (`character_likes`, `character_saves`, `character_image_likes`, `character_image_comments`, `character_creators`) — added to `CLIENT_ONLY_TABLES` in `scripts/dump-schema.ts`; new repo `src/database/repositories/characterSocial.ts`; 15 new unit tests; migration snapshots regenerated. The **Chats stat already counts chat sessions** (distinct interactions), not messages — no change needed. Changelog updated (Characters > Added). tsc clean; unit suites green.
- **Carousel Centering Fix (2026-08-13, DEVICE-CONFIRMED):** User reported cards settling off-center when swiping. Root cause: the first centering pass mixed two offset conventions — the `contentContainerStyle` padding already centers card 0 at scrollX=0, but `snapToInterval`/`getItemLayout`/interpolation inputRanges/tap-scroll all used `SNAP`(card+gap) as the item pitch while items were rendered `CARD_WIDTH` wide (no gap rendered) → every stop drifted by the gap. FIX in `CharacterCoverFlowCarousel.tsx`: each FlatList slot is now `ITEM_WIDTH` (card + gap) wide with the card centered inside (`cardSlot` style), `snapToOffsets={index*ITEM_WIDTH}` pins exact native snap points that match the interpolation peaks, `getItemLayout` uses the same pitch, the mount/filter-reset effect drives `scrollToOffset(initialIndex*ITEM_WIDTH)` (replacing `initialScrollIndex`, which left-aligns), and `handleScrollEnd` derives the index from `round(x/ITEM_WIDTH)` — no `edgePad` added anywhere since the padding already centers card 0 at x=0. User: "it is perfect now". tsc + eslint clean.
- **Duplicate AI Partner Flow — 3D Cover Flow Carousel (2026-08-13):**
- **Duplicate AI Partner Flow — 3D Cover Flow Carousel (2026-08-13):** Per user request, the AI card picker that opens when tapping an existing AI character card was upgraded from the 2-column card grid to a sleek **3D Vertical/Perspective Cover Flow carousel** (`src/components/characters/CharacterCoverFlowCarousel.tsx`, new). The active center card is highlighted at scale 1.0 with full opacity and an elevated neon glow (animated halo ring), while preceding/following cards are angled (16° rotateY), scaled down (0.85), dimmed (0.55 opacity) and recessed along the Z-axis (perspective 1100) — the classic stacked-deck cover-flow look. IMPLEMENTATION NOTE + ON-DEVICE RENDER-ERROR FIX: the project has NO react-native-reanimated / gesture-handler, so everything is RN built-in `Animated`. The FIRST implementation used `Animated.FlatList` + native-driver `Animated.event` (`useNativeDriver: true`) — which caused an on-device RENDER ERROR because attaching a native animated scroll event to a view inside a `<Modal>` (separate Android window/root) on the New Architecture throws at attach time (jest cannot reproduce it — the native animated module is a no-op mock there, which is why tsc/eslint/jest all passed while the device failed). FIXED by switching to a plain `FlatList` + JS-driver `Animated.event` (`useNativeDriver: false`), which recomputes per-card interpolations (translateX/translateY/scale/rotateY/opacity/glow) on the JS thread — perfectly fluid for a small deck and safe inside Modal. Rotation interpolation uses STRING degree units (`'16deg'`/`'0deg'`/`'-16deg'`) — `transformDataType` normalizes `deg`→radians for the native path (raw numbers would be misread as radians). Swipe = native FlatList paging (`snapToInterval`, `decelerationRate:'fast'`); tap = `scrollToOffset` to center + `onSelect` fires after the card settles (`onMomentumScrollEnd`/`onScrollEndDrag` commit + 420ms safety timer). Deck starts centered on the middle card (`initialScrollIndex` + `getItemLayout`, `edgePad` so first/last center too); dynamic zIndex layering; pagination dots + focused name under the deck; swipe-hint pill (new i18n `carouselSwipeHint`). Card anatomy: glassmorphism dark card (82% elevated bg), specular gradient hairline border, portrait avatar with bottom fade, name + persona tag (personality ?? description), gradient mode pip (copy/arrow), bottom accent stripe. The `AICardPickerModal` was rewritten to host the carousel (grid/FlatList/staggered-reveal removed; sheet entrance spring + search filter preserved — search filters the deck). Props: `profiles`/`images`/`mode`/`onSelect`; same `ai-picker-card-<id>` testIDs preserved. tsc clean; eslint clean; unit 43 tests green. Changelog updated (Characters > Added/Changed). Device-verify on next build.
- **Duplicate AI Partner Flow (2026-08-13):** Per user request, tapping an existing AI character card on the Characters screen now opens a smooth obsidian-glass card-picker sheet (`src/components/characters/AICardPickerModal.tsx`) — 2-column card grid (portrait + name + description), live search, springy sheet entrance, staggered per-card reveal, copy-badge pip. Picking a card navigates to `CreateAI` with the new `duplicateProfileId` route param. `CreateAIScreen` runs a duplicate prefill: `getNextEntityAliasCopy()` (new repo fn in `entities.ts` + pure `stripCopySuffix()` helper) computes the copy number — "<name> 02", "03", … skipping taken numbers, ignoring soft-deleted aliases, case-insensitive — and **continues the series when duplicating an already-numbered copy** ("Aria 02" → "Aria 03", never "Aria 02 02"). All detail fields + voice settings carry over; the primary avatar image is copied (base64 + mime); the source entity's module mapping is copied and pre-selected in the Advanced pickers once configs load; `base_prompt`/`scenario`/`example_dialogues`/`lifecycle_config`/`vision_config_id` also carry over on create. The copy is a brand-new profile + entity (alias = copied name) that can be edited, Saved, or Start Chatted. Card tap NO LONGER opens the profile editor — editing now lives in the long-press context menu (`CharacterCardMenuModal` gained `onEdit` + an "Edit Profile" row). The "From an Existing One" FAB flow now reuses the same picker in `mode='fromExisting'` (link mode → `prefillProfileId`). `ExistingProfilePickerModal.tsx` DELETED (superseded). New repo tests: `getNextEntityAliasCopy` suite (8) in `entities.test.ts` (29 total). i18n: `characters.json` gained `duplicateTitle`/`duplicateHint`/`duplicateSearch`/`duplicateEmpty`/`duplicateEmptyHint`/`duplicateCopied`/`duplicateTitleScreen`; `createAI.json` gained `duplicateTitle` ("Copy of {{name}}"). Changelog updated (Characters > Added/Changed). tsc clean; eslint clean (2 pre-existing warnings in CharactersScreen + 3 pre-existing errors in entities.test.ts).
- **CreateAI — Save Without Chatting (2026-08-13):** The Create AI Partner bottom actions are now two side-by-side buttons: **Save** (secondary, `content-save-outline` icon) and **Start Chatting** (primary). `handleCreate` was refactored into `createPartner(navigateToChat: boolean)` — `handleCreate` = `createPartner(true)` (persists + resolves persona + `navigation.replace('ChatDetail')`), `handleSave` = `createPartner(false)` (persists, then `navigation.goBack()`). Both share the full create pipeline (profile creation-or-link, entity, module mapping, Soulbits default configs when none selected, syncAndWait). New i18n key `save` in createAI.json; `ctaRow`/`ctaButton` styles (flex row, equal-width buttons).
- **CreateAI Restore — "From an Existing One" Flow (2026-08-13):** The "From an Existing One" option was restored to the Characters screen "New AI Partner" sheet (`CreatePartnerModal` gained `onFromExisting` prop + row). It opens a new `ExistingProfilePickerModal.tsx` (obsidian-glass sheet listing all character profiles with avatar/name/description), and picking one navigates to `CreateAI` with `{ prefillProfileId }`. `CreateAIScreen` re-gained `prefillProfileId` support: on mount it loads the profile via `getCharacterProfile` and prefills name/description/personality/appearance/backstory/voice/typing/audio fields; on create it LINKS that profile (no new character_profiles row, no new image) instead of creating a fresh one. `CreateAI` route param is `{ prefillProfileId?: string }` again; all `navigate('CreateAI')` call sites updated to `navigate('CreateAI', {})`.
- **CreateAI Follow-up — Voice Settings + Collapsible Details (2026-08-13):** Per user feedback on the CreateAI redesign, the Details card is now a collapsible dropdown (chevron toggle, `showDetails` state) and gained a **Voice & Behavior** subsection: voice characteristics, typing speed (WPM), and audio response chance (%) — all persisted to `character_profiles` on create with clamped ranges (WPM 1–200, audio 0–100). New i18n keys: `voiceBehaviorLabel`/`voiceLabel`/`voicePlaceholder`/`typingSpeedLabel`/`audioChanceLabel`. eslint + tsc clean.
- **Create AI Partner Redesign + Auto Soulbits Defaults (2026-08-13):** Per user request, the `CreateAI` screen was rebuilt as a 3-section wizard (the old profile-card carousel was REMOVED — `ProfilePickerCard.tsx` deleted, `CreateAI` route param simplified to `undefined`). Sections: **(1) General** — name (required), description, avatar picker; **(2) Details** — personality, appearance, backstory; **(3) Advanced** — collapsible, grouped into **AI Model** (backend/cognition), **Voice** (tts/stt), **Config** (rag/movement/vision/imagination) module pickers. **Name-only quick start:** when NO module config is selected at create time, `handleCreate` calls the new `src/services/SoulbitsDefaultConfigService.ts` (`ensureSoulbitsDefaultConfigs()`) which transparently creates a full Soulbits Cloud stack in the background — one provider config per module (LLM for backend/cognition/movement/vision, embeddings for rag, harmonyspeech TTS, whisper stt + silero-vad, model injected via `injectSoulbitsToken`) + one module config per table (name `"Soulbits Cloud (default)"`, all idempotent by name). The entity mapping then references these defaults so the partner is chat-ready with zero config. `createAI.json` rewritten with section labels (`sectionGeneral`/`sectionDetails`/`aiModelSection`/`voiceSection`/`configSection`), detail field labels, and `defaultConfigNote`; old carousel keys (profileLabel/profileHint/selectProfile/createNewProfile/noDescription) removed. The "From an Existing Profile" option was removed from the `CreatePartnerModal` (it depended on the deleted carousel) — the sheet now offers **New AI Partner** (→ CreateAI) and **Import Character Card**. `navigate('CreateAI', {})` call sites (Characters/ChatList/EntityConfig) updated to `navigate('CreateAI')`. Changelog updated (Characters > Added/Changed). tsc clean; eslint clean.
- **Characters — "New AI Partner" Sheet Replaces FAB Speed Dial (2026-08-13):** Per user request, the Characters screen **＋ FAB** no longer expands into a speed-dial (Create Profile / Import Card). It now opens a small obsidian-glass bottom sheet (`src/components/characters/CreatePartnerModal.tsx`) asking how to bring in a new AI partner: **New AI Partner** (navigates to the `CreateAI` wizard) or **Import Character Card** (existing PNG/JSON import flow, `handleImportCard` unchanged). The old speed-dial code (Animated expand/rotate, `expanded` state, `speedAction*`/`backdrop` styles, `create-profile-option`/`import-character-card` FAB testIDs) was REMOVED. `ThemedFab` gained an optional `testID` prop (additive, no behavior change). i18n: `characters.json` gained `createPartnerTitle`/`createPartnerMessage`/`createNewPartner`/`createNewPartnerHint`/`importCardHint`. `handleCreateNew` now navigates to `CreateAI` instead of `CharacterProfileEdit` — the empty-state "Create First Profile" button benefits too. Changelog updated (Characters > Changed). tsc clean; no tests reference the removed testIDs.
- **Haptic Feedback — Working Toggle + Full Primary-Button Coverage (2026-08-12):** The Settings "Haptic feedback" toggle now actually works. Root cause: `src/utils/haptics.ts` always vibrated and never read the persisted `@harmony_setting_haptic_feedback` preference. FIX: haptics module now owns the preference — `loadHapticPreference()` (called once in `App.tsx` on mount), `setHapticFeedbackEnabled()` (wired into `SettingsScreen`'s toggle), `isHapticFeedbackEnabled()`, and an in-memory gate inside `hapticLightPress()`/`hapticPulse()` (`hapticCancel()` deliberately ungated so an in-progress vibration still stops when the user disables). The gate is synchronous/in-memory so the very next press respects the toggle without an async re-read. ADDED haptics (`hapticLightPress`) to all primary buttons that lacked it: profile pills (Edit Profile / New Persona on `MyProfileScreen`), Characters speed-dial actions (Import/Create) + filter chips + manage chip, character-card chat + favorite buttons (`CharacterProfileCard`), chat send/mic/stop (`ChatInput`), header save buttons on `EntityConfigEdit`/`CharacterProfileEdit`/`ModuleConfigEdit`/`ThemeEditor`/`EmojiActionEditModal`, chat context menu + reply-mode + scroll-to-bottom (`ChatDetailScreen`), `PersonaSwitcherModal` rows, `PersonaRow` chat button, `AppAlertModal` action/cancel buttons, `CharacterCardMenuModal` + `ManageCategoriesModal` + `AddToCategoryModal` actions, `EmojiActionEditorScreen` FAB, `ChatListScreen` conversation rows + info button, `ProfileTabs`, `EntityModuleSelector` + edit pencil, `SelectPicker`, `FormField` eye toggle, `ConnectionSetupScreen` mode cards, `ChatBubble` edit/send/save/retry, `ThemeCard`/`EmojiStyleCard`, `LandingCard`, `EntityCard`, `Discover`/`Market` search-clear, `EmojiItem`, `CategoryTabBar`, `EmojiPickerInline`, `ProfileImagePicker`. New unit suite `src/utils/__tests__/haptics.test.ts` (8 tests) locks the gating contract. tsc clean; affected unit suites (AppAlert, CloudProvisioningCard, haptics) green. CHANGELOG updated (Navigation > Changed).
- **Characters Screen — Favorites + Custom Categories (2026-08-12):** Per user request, the My AI Characters screen now has a filter system so users can set favorites aside and organize characters into categories they choose. DATA: three new CLIENT-ONLY tables via migration 000039 (`character_categories`, `character_category_members`, `character_favorites`) — never synced to the engine (strict schema parity; `CLIENT_ONLY_TABLES` in scripts/dump-schema.ts extended). Repo fns in `src/database/repositories/characters.ts`: favorites (isCharacterFavorite/addCharacterFavorite/removeCharacterFavorite/toggleCharacterFavorite/getFavoriteCharacterProfileIds) + categories (getCharacterCategories/createCharacterCategory/renameCharacterCategory/deleteCharacterCategory/getCharacterCategoryMembers/addCharacterToCategory/removeCharacterFromCategory/getCharacterProfileCategories). UI on `CharactersScreen`: horizontal chip row under the search bar — **All** | **Favorites** | each user category | a **"Manage categories"** chip (plus-box icon + label, dashed border); `activeFilter` ('all' | 'favorites' | category id) drives filteredProfiles (combined with name/description search). Cards (`CharacterProfileCard`) gained a clean heart toggle only (bottom-left, filled accent when favorited; icon-only with text-shadow — NO circle backdrop). LONG-PRESS context menu (`CharacterCardMenuModal.tsx`): a card long-press opens a bottom sheet with **Add to category** (opens `AddToCategoryModal.tsx` checkbox picker) and **Delete** (keeps the existing confirm dialog). The per-category MEMBER EDITOR inside `ManageCategoriesModal.tsx` (a **plus** button that expanded a member list to toggle which characters belong) was REMOVED per user request — assigning characters to categories now happens ONLY via the long-press **Add to category** picker; `ManageCategoriesModal` keeps create/rename/delete. (Earlier the separate CategoryAssignModal was also REMOVED per user feedback — they disliked the shape button on the card.) SAFE-AREA FIX: the manage-categories sheet now applies `paddingBottom: safeBottom + 24` (via useSafeAreaInsets) so the category-name input is no longer hidden under Android navigation buttons. Empty states differentiate no-profiles / no-search-results / no-favorites / empty-category. Tab bar label: Characters tab is now **Characters** on the nav bar only (MainTabNavigator tabBarLabel + GlassTabBar FALLBACK_LABELS; header title stays "My AI Characters"). i18n keys added to characters.json (cardMenuAddToCategory/cardMenuDelete/addToCategoryTitle/addToCategoryNoCategories). Tests: +13 in characters.test.ts (favorites 5 + categories 7 + default). tsc clean; unit suites pass except the 2 documented pre-existing better-sqlite3 cross-worker contamination files (pass in isolation); eslint clean on new code (only pre-existing warnings). NOT device-verified yet.
- **My Profile Screen + Persona System (2026-08-10):** Per user request, the My Profile tab is now a complete professional profile. Layout: ScreenHeader (title + hamburger) -> hero (gradient-ring ProfileAvatar 112dp, bold display name, small NON-bold muted @username directly underneath) -> Edit Profile button (ThemedButton secondary -> EditProfile) -> stats ThemedCard (Followers 0 | Following 0 | AI Characters = count of getUserCharacterProfiles()) -> bio ThemedCard -> ProfileTabs segmented control (AI Characters | Posts | Saved; Posts/Saved = ThemedEmptyState) -> Personas section (SectionHeader + "New Persona" pill + ThemedCard of PersonaRows). All data is LOCAL-FIRST: cloud useAuth().user (base UserProfile now has OPTIONAL username/bio/avatar_url fields - backend /v1/auth/me does not return them yet) is merged with per-user AsyncStorage extras via new src/services/profile/UserProfileStore.ts (key @harmony_profile/<userId>, stores displayName/username/bio/avatar_data_url; MyProfileScreen resolves displayName as local displayName || cloud user.display_name || 'User', and EditProfileScreen persists the edited display name locally since there is no backend PATCH /v1/auth/me yet). New screens: src/screens/EditProfileScreen.tsx (display name, username with @ prefix, bio, avatar picker via launchImageLibrary inside withExternalFlow; saves via UserProfileStore) and src/screens/PersonaEditScreen.tsx (create/edit a persona = Entity + linked CharacterProfile tagged source='user'; fields name/description/personality/picture; create path generates generateId() profile + entity alias=name with UNIQUE-alias conflict guard, setCharacterProfileSource(profileId,'user'), empty module mapping, syncService.syncAndWait best-effort before goBack; edit path updateCharacterProfile + updateEntity + primary-image replace/remove via deleteCharacterImage+createCharacterImage; delete path deleteEntity). Both routes registered in AppNavigator (EditProfile: undefined, PersonaEdit: {entityId?: string} | undefined); MainTabs now typed NavigatorScreenParams<MainTabParamList> | undefined. New shared components: src/components/profile/ProfileAvatar.tsx (gradient-ring avatar with initials fallback), PersonaRow.tsx (avatar/name/desc + ACTIVE chip + chat-as button + chevron), ProfileTabs.tsx (segmented pills). New repo fns: getUserPersonas/getPersonaByEntityId in src/database/repositories/personas.ts (entities INNER JOIN character_profiles INNER JOIN character_profile_sources WHERE source='user', avatar via getPrimaryImage+imageToDataURL) and getUserCharacterProfiles in characters.ts (mirrors getCommunityCharacterProfiles with INNER JOIN source='user'). Persona "chat as" button calls ChatPreferencesService.setGlobalImpersonatedEntity + updates the ACTIVE chip + shows toast - re-introducing the persona selector removed from ChatList header on this screen. SIGN-IN REDIRECT: AuthContext gained signInVersion (monotonic counter bumped after login/loginWithGoogle/loginWithApple only - NOT on bootstrap/refresh), exposed in the context value; App.tsx AppShell observes it via a prevSignInVersionRef and on change navigationRef.navigate('MainTabs', { screen: 'MyProfile' }) (100ms delay) so a fresh sign-in lands the user on their profile. Unauthenticated state: MyProfile tab shows ThemedEmptyState + "Sign In" button -> Login. i18n: profile.json expanded with all hero/stats/tabs/persona/edit keys. Changelog updated (new "Profile" section). tsc --noEmit clean; unit 46 suites / 438 tests green; NOT yet device-verified; no unit tests reference the new screens.
- **Chat List — "Chatting as" Pill + Identity-Settings Button Removed (2026-08-10):** Per user request, removed BOTH the "Chatting as" persona pill (with its gradient-ring avatar, name, chevron, `ImpersonationSelectorModal` trigger) AND the adjacent identity-settings ⚙ cog button (`edit-my-identity-button` → `EntityConfigEdit`) from the ChatListScreen header. PRESERVED for later re-introduction on another screen: `src/components/modals/ImpersonationSelectorModal.tsx`, `HeaderMenuButton` (still in the header), `ChatPreferencesService`, the i18n keys in `chatList.json` (`chattingAs`, `editMyIdentity`, `editIdentityAction`), and the underlying global impersonation state. `ChatListScreen` still loads the stored persona (`loadImpersonatedEntity` + `ChatPreferencesService.getGlobalImpersonatedEntity`) and drives the conversation list with it — only the header UI entry points are gone. Removed dead code: `ImpersonationSelectorModal` import/render, `handleImpersonationSelect`, `handleEditMyIdentity`, `selectorModalVisible`, `impersonatedEntityDisplay` state, and the `impersonationPill*`/`myIdentityButton` styles. tsc clean; no tests reference the removed test IDs. Changelog updated (Navigation > Changed). The persona selector will live on a future screen (e.g. Settings).
- **Unified Empty-State Design Across App (2026-08-10):** All "nothing here yet"/0-results/not-paired/empty states now share the Market screen's premium design via a new shared component `src/components/themed/ThemedEmptyState.tsx` — gradient icon ring (`ThemedGradient primary` + elevated inner circle) with bold title + muted hint + optional action. Applied to: CharactersScreen (no profiles / no results), DiscoverScreen (no characters / no results — overlay), ChatListScreen (no conversations / not-paired / cloud-preparing), EntityConfigScreen (no entities), EmojiActionEditorScreen (no actions), EmojiGrid (emoji search 0-results, compact). MarketScreen refactored to consume the same component. All states are consistently sized and positioned right below the header (removed `justifyContent:'center'` from `emptyListContent` in Characters/EntityConfig so they sit at the top like Market instead of vertically centered). Supports `testID`/`accessibilityLabel` passthrough (kept `chat-list-empty`, `chat-list-not-paired`, `chat-list-cloud-preparing`, `open-connection-manager-button`, `connect-now-button` for Maestro). tsc --noEmit clean; no unit tests reference these screens. Changelog updated (Navigation > Changed).
- **Settings Moved Out of Tab Bar → Header Hamburger Menu (2026-08-10):** The Settings entry is no longer a bottom tab. Removed the `Settings` route from `MainTabNavigator` + `GlassTabBar` (TAB_ICONS/FALLBACK_LABELS) — the tab bar is now 4 items: Chat | Discover [CENTER] | Characters | Market. `SettingsScreen` is now a root-stack route in `AppNavigator` (pushed over the tabs, gets a back button via `ScreenHeader onBack`). New `src/components/navigation/HeaderMenuButton.tsx` renders a compact obsidian-glass circular "three lines" (☰) button (accent lines on glass pill, haptic + theme-aware). Tapping it navigates DIRECTLY to the full `SettingsScreen` (per user request — the old `SettingsMenu` modal was deleted as "the shitty old window"). `HeaderMenuButton` is mounted in the header (`ScreenHeader right`) of all 4 tab screens: ChatList (in the existing header-right row), Discover, Characters, and Market. The orphaned `navigation` i18n namespace was also removed (its only consumer `SettingsMenu` was deleted; `navigation.json` deleted + `I18nContext` cleaned). E2E: `_shared/navigate-to-sync-settings.yaml` now taps `header-menu-button` → `settings-sync-card` (was `tab-settings` + `settings-sync-card`). Docs updated: CHANGELOG (Navigation > Changed), .planning/codebase ARCHITECTURE+STRUCTURE, reports. tsc --noEmit clean; no unit tests reference GlassTabBar/SettingsMenu.
- **Discover — Other-Users'-Characters Grid + Search (2026-08-10):** Discover now shows ONLY AI characters created by OTHER users (those synced DOWN from the engine — they arrive with no source tag → default 'community'). The CURRENT user's own characters — created via Create AI, the profile editor, or imported character cards — are tagged 'user' and hidden from their own Discover (they still sync UP to the engine and appear on other users' Discover grids). Because `getChangedRecords` reads `character_profiles` via `SELECT *` and syncs all columns to the engine (strict schema parity per docs/schema-parity.md), a `source` column on character_profiles would break parity — so source tracking lives in a CLIENT-ONLY sidecar table `character_profile_sources` (migration 000035 creates the table; 000036 backfills pre-existing profiles as 'user' so existing installs hide their current characters). New repo fns in `src/database/repositories/characters.ts`: `setCharacterProfileSource`, `getCharacterProfileSource`, `getCommunityCharacterProfiles` (LEFT JOIN sidecar, filters out 'user'). `CreateAIScreen` + `CharacterProfileEditScreen` (create path) + `CharacterCardImportService` (import path) all call `setCharacterProfileSource(profileId, 'user')` after creation; ONLY the sync-down path stays untagged → community. DiscoverScreen loads via `getCommunityCharacterProfiles` (not `getAllCharacterProfiles`), searches by NAME only (case-insensitive; description matching was removed because long descriptions matched almost any letter and hid the 0-results state), and renders a guaranteed-visible overlay (NOT ListEmptyComponent — unreliable with numColumns>1) for loading/0-results/empty states. i18n `discover.json`: searchPlaceholder, noResults("No results found for \"{{query}}\""), noResultsHint, noCharacters("No characters to discover yet"), noCharactersHint("Characters created by other users will show up here."), chatOpenFailed. Schema parity: `scripts/dump-schema.ts` now excludes CLIENT_ONLY_TABLES (`character_profile_sources`) from the RN dump so the RN↔Go parity gate stays green; `schema/rn-schema.json` regenerated. Characters tab unchanged (shows everything incl. user-created). Migration snapshots updated; tsc clean; unit 46 suites green (intermittent better-sqlite3 cross-worker flakes in nodeSide/nodeDatabase.smoke/cross-repo are pre-existing and pass in isolation/reruns). Changelog updated under Navigation > Changed.
- **Search Tab Merged Into Discover (2026-08-10):** Removed the dedicated Search tab (SearchScreen.tsx + search.json + tab-search testID + nav.search i18n key) per user request. The Discover tab (`src/screens/DiscoverScreen.tsx`) now renders a glass search bar injected into the ScreenHeader and a 2-column grid of AI character profiles (same `CharacterProfileCard` + `getAllCharacterProfiles`/`getCharacterImages` repos as CharactersScreen), filtered live by name/description. Tapping a card opens `CharacterProfileEdit`, and the card's chat button runs the full `CharactersScreen.handleChatPress` flow (resolve impersonated entity → reuse/create entity via `getEntityByCharacterProfileId` → `syncService.syncAndWait` on new entities → derive participantKey/scope → navigate to ChatDetail with temp UUIDv7). 4-tab bar now: Discover | Chat [CENTER] | Characters | Settings — GlassTabBar TAB_ICONS/FALLBACK_LABELS, MainTabParamList, SettingsMenu nav item, and I18nContext (import/resource/ns) all updated. i18n: discover.json gained searchPlaceholder/noResults/noResultsHint/noCharacters/noCharactersHint/chatOpenFailed; navigation.json dropped nav.search. Comments + codebase maps (.planning) + reports updated. tsc --noEmit clean; unit 44 suites pass (2 pre-existing DB rollback test suites failing — unrelated). Changelog updated under Navigation > Changed.
- **Module Config — Simplified UX + Soulbits Cloud Consistency (2026-08-08, Task 3 of Dom's feedback):** Three items on `ModuleConfigEditScreen` + constants. (1) PROVIDER LIST AUDIT — `src/constants/moduleConfiguration.ts`: added `soulbitscloud` to the RAG, STT, and TTS `providerOptions` (they were missing it even though the model catalog supports embeddings/whisper/tts models; regression guard in new `moduleConfiguration.test.ts` asserts every module with a `SOULBITS_MODEL_QUERY` also lists Soulbits Cloud). (2) VAD MODEL MAPPING — new `vad` entry in `SOULBITS_MODEL_QUERY` (`model_type: 'vad'`) + `SOULBITS_FALLBACK_MODELS.vad = ['silero-vad']` (real live-catalog id); `renderInlineProviderFields` now resolves the model-select module type via `modelModuleType = slot === 'vad' ? 'vad' : moduleType` so the VAD slot queries voice-activity models instead of STT transcription models. (3) CONDITIONAL FIELD VISIBILITY — when a managed Soulbits Cloud provider is selected AND the app is in cloud mode (`connectionStatus.mode === 'cloud'`), the endpoint (`base_url`/`endpoint`) and credential (`api_key`/`api_token`) fields are HIDDEN in both Simple and Advanced modes (auto-synced from backend; injected via `injectSoulbitsToken`, synced via `soulbitsTokenSync`). New pure helper module `src/constants/moduleConfigVisibility.ts` (`isSimpleFieldKey`, `isManagedCloudField`, `SIMPLE_FIELD_KEYS`) unit-tested. (4) SIMPLE VS ADVANCED TOGGLE — COMPACT SEGMENTED CONTROL (Simple | Advanced pills, accent-filled active segment) in a slim card below General (FOLLOW-UP: user rejected the first full-card chevron toggle as "ugly + buggy" — the bug was a side-effect inside the state updater; fixed by persisting via a separate `useEffect([showAdvanced])`). Simple mode shows only `SIMPLE_FIELD_KEYS` (name/credentials/endpoint/model/voice/speed/format…) + hides `AdvancedSamplingParams`; Advanced mode shows the full schema. Preference persisted in AsyncStorage key `module_config_show_advanced` (restored on mount, written on toggle). i18n keys `modeLabel`/`modeSimple`/`modeAdvanced`/`modeSimpleHint`/`modeAdvancedHint` in `moduleConfig.json`. (5) PLUS-BUTTON REMOVAL (FOLLOW-UP: user disliked the trailing "+" per module selector) — `EntityModuleSelectorWithActions` no longer renders a create "+" button; `EntityModuleSelector` gained an optional `onCreateNew` prop that renders a "Create new config…" row as the last sheet item (plus-circle-outline icon, accent), and only the pencil edit button remains beside the selector when a config is selected. Verification: tsc clean; unit 46 suites / 430 tests green (new: `moduleConfigVisibility.test.ts` 5, `moduleConfiguration.test.ts` 3, `soulbitsModels.test.ts` +vad cases); integration 9 suites / 46 pass + 1 skip; eslint only pre-existing 3 errors (exhaustive-deps ×2, unused `moduleSpecificDefaults`) + 6 inline-style warnings (3 pre-existing in EntityModuleSelector). Changelog updated. NOT yet device-verified; no test files render ModuleConfigEditScreen or the selectors.
- **Characters Screen — Chat Button on Character Cards (2026-08-08):** Each `CharacterProfileCard` on the Characters screen now shows a floating chat button (accent gradient pill, top-right over the portrait) that opens a direct chat with that character. ChatDetail requires an ENTITY linked to the character profile plus the impersonated "user" entity, so `CharactersScreen.handleChatPress` resolves the global impersonated entity (same logic as CreateAIScreen), looks up an entity via the new repository function `getEntityByCharacterProfileId` (returns most-recently-created active entity; added to `src/database/repositories/entities.ts` with 4 unit tests), and if none exists creates one (id + alias = profile name, links `character_profile_id`, empty module mapping) and AWAITS `syncService.syncAndWait` before navigating (engine must ingest the entity before INIT_ENTITY or chat sticks on "Connecting..."). Then derives participantKey via `deriveParticipantKey`/`deriveScopeFromParticipants`, generates a temp UUIDv7 interactionId (ChatDetail replaces it with the canonical server ID) and navigates to `ChatDetail`. The card button stops press propagation so it never triggers the card's edit/long-press handlers. i18n `chatOpenFailed` added to characters.json. tsc clean; unit 44 suites / 421 tests green (4 new for the repository lookup). Changelog updated. NOT yet device-verified; no test files reference CharactersScreen.
- **Create AI — Visual Profile Picker Carousel (2026-08-08):** Per Dom's feedback, the "create a character from an existing profile" flow no longer uses a plain text dropdown (avatar only appeared after selection — felt like picking a DB row). Replaced with a horizontal card carousel in the Character Profile card on `CreateAIScreen`: each card (`src/components/characters/ProfilePickerCard.tsx`) mirrors the Obsidian Glass anatomy of `CharacterProfileCard` (Characters screen) — 3:4 portrait with dark fade overlay, theme-glass body, hairline gradient border that becomes a 2px accent ring when selected, ambient glow (intensified when selected), name + description snippet, left accent stripe, plus a check badge on the selected card. The "Create new profile" card is the first item with a ＋ placeholder. All profiles' primary images are now loaded up front into a `profileImages` map (was: only the selected profile's image was fetched after selection) so avatars show BEFORE confirming. Selecting a card prefills Name/Personality and centers the hero preview; the carousel auto-scrolls to center the selected card on layout (ref + `scrollToIndex`, `onScrollToIndexFailed` guard). Switching back to "Create new profile" resets the prefilled identity fields (name/personality) — fixed a regression where the previously selected card's name stuck around. Removed the bottom-sheet Modal + its styles. New i18n key `createNewProfileHint`. tsc clean; eslint only pre-existing warning. Changelog updated. NOT yet device-verified; no test files reference CreateAIScreen.
- **Chat Lifecycle — Stuck-"connecting" Strand + Monkey Test (2026-08-05, on-device-diagnosed):** Re-test after Fix A (dedup) + Fix C (participant-set connection ids) confirmed both working (single sync per switch, less flaky, distinct `entity-user-<pk>` sockets). Residual: a Marcella session briefly stuck in "connecting" ~15s then resolved via the init retry. Root cause (adb timeline): `INIT_ENTITY SUCCESS` for the partner entity arrived BEFORE `startInteractionSession` registered the session in `this.sessions` (registration was AFTER the parallel connect+send `Promise.all`); `handleEntityEvent → handleInitEntityResponse` then ran with `interactionSession === null`, so the connection was never marked 'active' → the session never reached all-active → stalled until the 15s init timer retried. FIX: moved `this.sessions.set(tempInteractionId, session)` to BEFORE the parallel send loop (connections map is shared by ref, so entries added below stay visible) + `this.sessions.delete(tempInteractionId)` in the catch on failure. Added a deterministic seeded "monkey" stress test `entitySessionMonkey.test.ts` (40 rapid Marcella↔Claire switches, simulated backend responds ~50% SYNCHRONOUSLY during sendEvent + 50% via randomized timers — the sync case is the race window). Invariant: exactly one `session:started` per start + no leaks + no throws. COUNTER-TEST PROVEN by user request: with the fix reverted the monkey test FAILS (9/40 starts completed — the rest stranded); with the fix restored it PASSES (40/40). This validates both the strand fix AND the dedup guard (double-emit would exceed 40). Unit 43→44 suites / 416→417 tests; tsc clean; `--maxWorkers` 45 still ≥ 44. UNCOMMITTED (user staged the prior work).
- **Chat Lifecycle — Duplicate session:started + Participant-Set Connection IDs (2026-08-05, on-device-diagnosed Fixes A & C):** On-device re-test of the cascade fix (above) surfaced two NEW interaction-session bugs (distinct from the cascade, which is fixed — no crash/ping-pong/retry-loop). (A) DUPLICATE `session:started` → double sync per chat-open: `EntitySessionService.handleInitEntityResponse` emitted `session:started` (+ the on-start `initiateSync`) with NO guard; both participants' INIT_ENTITY SUCCESS responses raced through the own-entity's `await createInteraction(...)` window and BOTH re-entered the all-active check after both connections were already 'active' → emit twice + 2 syncs (adb: `session:started`→`Initiating sync`×2, 1ms apart). FIX: added `InteractionSession.started` flag, emit/sync only when `allActive && !started`. (C) `entity-${entityId}` COLLISION across participant sets: the engine keys sessions by participant set (one canonical interaction_id per distinct participant set — user's earlier engine fix), but the app used a single `entity-user` slot for every chat → rapid Marcella↔Claire switching raced the previous `entity-user` teardown against the new `entity-user` setup → native "Already Connected"/stale-socket → 15s init timeouts → "Scheduling retry 1/3…3/3" connection delay. FIX: connection id now `entity-${entityId}-${participantKey}` (participantKey from deriveParticipantKey, '+' sanitized to '-' for URL safety in `?connection_id=`); `ConnectionManager.getEntityConnection` (unused) switched to scan by stored entityId. Event routing unaffected (uses ConnectionInfo.entityId, not id parsing). B (sync-on-start + sync-on-message) is INTENTIONAL per user — tuning is a separate concern. D (session-map isolation on canonical-id reuse) deferred per user ("not necessary once A lands") — rundown provided. Tests: new `entitySessionStartedDedup.test.ts` (2) + `entitySessionConnectionId.test.ts` (2); unit 41→43 suites / 412→416 tests; tsc clean; `--maxWorkers` 45 still ≥ 43. UNCOMMITTED with the cascade fix.
- **Chat Cascade Fix — Sibling Connection Coverage + Timeout-Branch Hardening (2026-08-05 follow-up):** Code review of the cascade fix (next bullet) confirmed the bug class is UNIQUE to the self-signed native module — the 3 sibling WS modes (`Unencrypted`/`Secure`/`Cloud`) use the standard browser `new WebSocket(url)` API (no listener-overwrite-without-unsubscribe, no synthetic close-error on a dead socket, no per-URL `getInstance` singleton) so they need NO equivalent fix; entity+sync both route through `InsecureSSLWebSocketConnection` for the local self-signed engine (entity mode = `ConnectionStateManager.getSecurityMode()`, same as sync). Filled the prior test-coverage GAP: new `src/services/websocket/__tests__/helpers/MockWebSocket.ts` (browser-style WS mock with `OPEN/CONNECTING/CLOSING/CLOSED` statics, `fire*` test helpers, real-`setImmediate` `flushMicrotasks()` that survives `jest.useFakeTimers()`) + 3 new suites — `UnencryptedWebSocketConnection.test.ts` (8: connect/error/reconnect-replace/server-close/send-guard/disconnect), `SecureWebSocketConnection.test.ts` (6: JWT subprotocol, missing-JWT, cert-vs-generic error routing, 10s timeout, reconnect), `CloudWebSocketConnection.test.ts` (7: PASETO subprotocol, not-authenticated, connect-failure-rejects-WITHOUT-disconnected, 1008 auth-refresh, established close, 15s timeout, settled-guard-no-double-settle). HARDENED observation 1 (was a bounded wart): the InsecureSSL `connect()` timeout branch settled the promise WITHOUT marking `failureSignalled`, so a delayed native onError leaked one extra `error`; root cause was SCOPE — `failureSignalled` was declared inside the `try` block (invisible to the timeout callback in the outer Promise-executor scope). Hoisted the declaration into the executor scope and set it in the timeout branch → timeout/onError/.catch now share ONE settle guard. New InsecureSSL characterization test pins it (timeout rejects exactly once, late error suppressed, next connect recovers). Impact analysis: LOW (interface-dispatched; 2 connection-establishment processes). Unit 38→41 suites / 390→412 tests; `--maxWorkers` bumped 40→45 in `package.json` + `docs/TESTING.md` (41 unit files now exceeds the documented 40-file better-sqlite3 cross-worker contamination threshold); tsc clean; `npm run test:unit` green.
- **Chat Connection Cascade Fix — Session-Stop Leak + WS Error Ping-Pong Crash (2026-08-05, logcat+docker-diagnosed):** User report: after the engine-side participant-matching fix, Marcella chat worked but Claire chat stuck in "Connecting...", then retries + error toast, escalating to an app crash. Diagnosed via adb logcat + docker logs + code: FOUR interacting app-side bugs. (1) TRIGGER — connection leak on chat exit: `EntitySessionService.stopInteractionSession` ran `await AudioPlayer.stop()` FIRST in its try-block; it throws `player_not_initialized` when TrackPlayer was never set up (no audio played in that chat) → outer catch swallowed → connection-teardown loop NEVER ran → every chat exit leaked its entity WebSockets ("Connection entity-X already exists, disconnecting first" on next session; "Received event for unknown entity" heartbeats on session-less connections; orphaned engine-side sessions). FIX: AudioPlayer.stop isolated in its own try/catch + per-connection fault isolation in the teardown loop. (2) AMPLIFIER/CRASH — `react-native-websocket-self-signed` JS `onX()` methods overwrite `listeners[event]` WITHOUT removing the previous native subscription; `InsecureSSLWebSocketConnection` "cleared" handlers by registering no-ops → orphaned real closures kept answering native onError with `ws.close()`; native `close()` on a missing socket emits ANOTHER `onError("No active WebSocket for this URL")` → self-sustaining ping-pong flood (2 events → hundreds/sec) → JNI global-ref table overflow (`art::JavaVMExt::AddGlobalRef` abort) = the crash. Same mechanism killed fresh sockets mid-handshake ("Socket is closed" on entity-user) — why Claire's connect failed instantly while Marcella (fewer orphans then) squeaked through. FIX: remove-then-register (`removeOn*Listener()`) in the connect prologue + after getInstance; benign "No active WebSocket for this URL" ignored (no close/emit/reject); `failureSignalled` guard dedups error emit+close per connect attempt. USER CONSTRAINT: never edit node_modules (no patch-package) — residual native quirks (no onClosed map cleanup → "Already Connected" costs one extra retry; close() on missing socket emits onError) mitigated JS-side; upstream patch documented in docs/future-work.md #24. (3) LOOP — retry counter wipe: the `preserveRetryState` fix only covered the clear in `startInteractionSession`; `retryInitialization`'s cleanup `stopInteractionSession` unconditionally deleted the participant-key retry state ("Cancelled pending fallback retry") → counter reset → "Scheduling retry 1/3" forever (~110 cycles/2min hammering the engine with connect/disconnect). FIX: snapshot/restore retry state around the cleanup stop. (4) Tests: new `entitySessionStop.test.ts` (AudioPlayer throw still disconnects all + per-conn fault isolation), InsecureSSL suite rewritten (self-firing fake: default connect fires captured onOpen + resolves) + 4 new regression tests, retry suite +1 (lingering-session cleanup path — reproduces the exact device flow). Unit: 38 suites / 390 tests green; tsc clean. GitNexus detect_changes: 3 expected processes only. UNCOMMITTED. Device verify pending: rebuild → Marcella↔Claire switching, watch logcat for absent "No active WebSocket" flood + escalating "Scheduling retry 2/3…3/3" then give-up.
- **Sync Name-Clash Keep ID-Cascade + stt_configs Dual-Provider Fix (2026-08-04, DEVICE-VERIFIED):** Three sequential device/test-reported bugs in the name-clash "keep" path, all fixed TDD and confirmed working on the emulator by the user ("sync fix works now with the ID rewrite").
  - **Bug 1 — keep never re-sent the adopted record.** `applyIdAdoption` (keep branch, `SyncService.ts`) adopted the server id into the local module-config row but the SQL only set `id` — no `updated_at` bump and provider_config_id still pointed at the LOCAL provider id. App's ONLY dirty-tracking is `getChangedRecords` (created_at/updated_at/deleted_at > lastSync) → row stayed invisible → never pushed; the `serverRecordIds` filter would also suppress it. Pulled the emulator DB (`ai.soulbits.chat.dev`, `harmony.db` via `adb exec-out run-as ... cat` — PS redirection corrupts binaries) — kept rows still showed the original 21:14:17Z `updated_at`. FIX: keep branch now `UPDATE ... SET id=?, updated_at=?, provider_config_id=?` (per ref) AND keep-resolved keys are DELETED from `serverRecordIds` after commit so the record re-sends as `update`. New integration test: `keep re-sends the adopted record with a bumped updated_at and the server provider_config_id`.
  - **Bug 2 — keep did not cascade to the app's local provider configs.** "keep" must preserve the COMPLETE app setup (module config + its provider config); IDs must match the incoming data so it can sync back. First draft (copy local values ONTO the server provider row + hard-delete the local row) FAILED in the test harness: NodeDatabase executes statements synchronously but defers success callbacks to microtasks → nested-callback SELECT/UPDATE/DELETE ran after COMMIT → "Failed to apply server data". REDESIGNED to RENAME semantics (per user's clarifying question): `cascadeKeepProviderConfig` RENAMES the LOCAL `provider_config_*` row to the server provider-config id (values + created_at preserved), bumps updated_at, SKIPS the incoming duplicate via `keptProviderServerIds` (rename can't collide on PK), re-points remaining module-config refs, re-sends the provider config. `keepCascadedProviderKeys` Set (cleared in `initiateSync` + on rollback; keys un-excluded from `serverRecordIds` post-commit). All cascade SQL issued SYNCHRONOUSLY inside the tx — no callback nesting; local provider refs are CAPTURED pre-transaction in `buildApplyPlan` from `clash.localRecord` (no in-transaction SELECT). Test: `keep renames the local provider config to the server id and re-sends it`.
  - **Bug 3 — device `failed to apply server data`: `stt_configs` has NO `provider_config_id` column.** Live adb logcat showed `SQLiteException: no such column: provider_config_id ... UPDATE stt_configs SET provider_config_id = ?` → whole apply tx rolled back. PRAGMA table_info on the pulled device DB: `stt_configs` has TWO provider refs — `transcription_provider`/`transcription_provider_config_id` AND `vad_provider`/`vad_provider_config_id` (all NOT NULL). My earlier schema check used a naive regex that falsely matched the `…_provider_config_id` SUBSTRING. FIX: `PROVIDER_CONFIG_REFERENCES` per-table map in `syncNameClash.ts` (`stt_configs` → both transcription+vad pairs; the other 7 module-config tables → single `provider_config_id`/`provider`); `NameClashApplyDirective` keep variant now carries `providerRefs: ReadonlyArray<KeepProviderRefCapture>` = `{configColumn, providerColumn, localProviderConfigId, localProvider}`; `applyIdAdoption` builds a per-ref UPDATE + `keptProviderServerIds` from the captured refs; `cascadeKeepProviderConfig` remap loop iterates the map (both stt columns). Engine `stt_configs` schema (migration 000031) confirmed IDENTICAL — incoming records carry the same columns. Regression test: `keep cascades BOTH stt_configs provider references (transcription + vad) to the server ids and re-sends them` (reproduces the device failure; would hit "no such column" pre-fix).
  - Verification: unit 377/377 (35 suites — `--maxWorkers` bumped 16→40 in package.json + docs/TESTING.md when the 35th unit file crossed the documented better-sqlite3 cross-worker contamination threshold); integration 46 pass + 1 skip (9 suites); name-clash suite 12 tests; tsc clean. All UNCOMMITTED with the rest of the name-clash WIP.
- **Sync Name-Clash Resolution Feature (2026-08-04):** Engine re-sends module-config rows (same `name`, different UUID) after an engine-side seed/restore or second device creates them; on the app the incoming INSERT would violate the DB-level UNIQUE name constraint → sync transaction abort → permanent "failed to apply server data" loop. Implemented app-side clash resolution: `src/services/syncNameClash.ts` (pure) + wiring in `SyncService.ts` (applyBufferedSyncData → buildApplyPlan → resolveNameClash) + `SyncSettingsScreen` dialog (`sync:name_clash` event, "Apply to all" checkbox). Modes: **overwrite** (server wins — adopt server id + values, remap local refs), **keep** (app wins — adopt server id, keep local values, remap refs, re-send; see cascade entry above), **rename** (local keeps its id, renamed `"X (YYYY-MM-DDTHH:MM:SS)"` via `generateRenamedName`, server record inserted). Clash detection spans the 8 `NAME_UNIQUE_TABLES` (backend/cognition/movement/rag/stt/tts/vision/imagination configs) + `CONFIG_ID_REFERENCES` (module configs referencing provider_config_id, entities.character_profile_id, character_profiles.vision_config_id). Migration 000034: rebuild `vision_configs` + `imagination_configs` with `name TEXT NOT NULL UNIQUE` (the 6 others already enforced it) — `_new`-table rebuild wrapped in `PRAGMA foreign_keys=OFF/ON` because `character_profiles.vision_config_id` FK (ON DELETE SET NULL) would fire on a plain DROP TABLE; mirrors harmony-link-private migration 000034 (schema parity; the migration runner disables FKs on HL too). Unit `syncNameClash.test.ts` + 12 integration tests.
- **AppAlert Popup Race Guard (2026-08-04):** Observed popup vanishing on fast dismiss/replace — `AppAlertContext.handleDismiss` ran `setTimeout(() => setConfig(null), 200)` and unconditionally cleared whatever config was current when the timer fired (e.g. a NEW alert opened within 200ms was wiped). Fix: generation guard — capture `dismissedConfig = pendingRef.current` at dismiss time; the delayed `setConfig(null)` only runs if `pendingRef.current === dismissedConfig`. Tests: `AppAlertContext.test.tsx` 2/2 green (debug mock marker later removed).
- **updated_at Bump on All Config Updates — Sync Dirty-Tracking Fix (2026-08-04):** Cross-repo fix with harmony-link-private. Root cause of "API key edits never reach the engine" (engine kept returning 401 "authorization header required" on all SoulbitsCloud calls): the app's ONLY change detection is `getChangedRecords` (`database/sync.ts`) — selects rows where `created_at`/`updated_at`/`deleted_at` > lastSync — but `update<Provider>Config` and `update<Module>Config` SQL never set `updated_at`, so edited rows stayed invisible to sync and were never pushed. Fixed: appended `updated_at = CURRENT_TIMESTAMP` to the SET clause of ALL 16 `update<Provider>Config` functions (`src/database/repositories/providers/*.ts`: openai, ollama, openaicompatible, openrouter, harmonyspeech, elevenlabs, kindroid, kajiwoto, characterai, localai, mistral, comfyui, xai, google, anthropic, soulbitscloud) and all 8 `update<Module>Config` functions (`src/database/repositories/modules.ts`: backend, cognition, movement, rag, stt, tts, vision, imagination). NOT touched: `updateAllSoulbitsCloudApiKeys` (already bumps), create/delete/soft-delete paths, `sync.ts`, `SyncService.ts`. Tests (TDD red→green): `providers.test.ts` +5 (bumps updated_at for OpenAI/Ollama/Anthropic/SoulbitsCloud + end-to-end regression: backdated row NOT dirty before edit, IS dirty after), `modules.test.ts` +2 (backend + cognition). Unit: 33 suites / 366 tests green; eslint clean. Verified against live data: engine DB `provider_config_soulbitscloud.api_key` len 0 vs app DB len 53 (`sb_cloud_g*`) — identical rows/UUIDs (21:14:17). Engine-side companions (harmony-link-private): `syncProviderRecord` LWW tie-break (equal updated_at → incoming wins, matches app `>=`), cloud-mode seeder empty-token guard (was wiping keys + re-stamping updated_at at every boot without a token), entity-cache refresh after seed in `run.go`. Changes UNCOMMITTED alongside existing WIP — user to decide commit split.
- Sync Size-Estimate Confirmation + Toast-Suppression (2026-08-03): Cross-repo sync QoL work with harmony-link-private (engine). Three items:
  - (1) Engine size-estimate correctness (ENGINE repo): fixed the estimate/fetch mismatch — bare `strftime('%s', updated_at)` returned TEXT storage class so SQLite compared TEXT vs INTEGER watermark as "TEXT > any INTEGER" → every row counted (estimate said 7 records, fetch sent 0). Shared WHERE predicates `whereChangedSince`/`whereChangedSinceNoDeletedAt` in `database/sync_utils.go` now back all 32 `queryGetChanged*` fetch constants AND the counters (`CountChangedRecords` with internal table-name switch for 2-column `emotion_state` vs 3-column tables; `CountChangedCharacterImages` returns count+bytes). `sendSizeEstimate` covers all 33 tables incl. previously-missing `interactions`/`emotion_state`/`memories`; image-count query error no longer discarded. Verification: `go build ./...` + `go test ./database/ ./eventserver/` green (9/9 sync suite).
  - (2) Estimate confirmation protocol (ENGINE + APP): engine now BLOCKS after emitting `SYNC_DATA_SIZE_ESTIMATE` until the app replies with the new `SYNC_DATA_SIZE_ESTIMATE_CONFIRM` event (`{sync_session_id, event_id, status: SUCCESS|REJECTED}`), 60s timeout → engine sends `SYNC_REJECT` + aborts, NO data transmitted without confirmation. App-side: `SyncService.handleSizeEstimate`/`confirmSizeEstimate`/`pendingSizeEstimate` + new `sync:estimate`/`sync:estimate:confirm` events; `SyncConnectionContext` shows a themed dialog (Cancel→reject/Confirm→accept, `blockBackdropDismiss: true`) when the estimate exceeds the threshold or on initial sync. Empty estimates auto-confirm silently. Backward compatible (no estimate → sync proceeds as before).
  - (3) Toast suppression during expected handshake (APP): new pure gate `shouldShowConnectionErrorToast({isTransportSettled, isReconnecting, reconnectAttempt, isCertFlowActive})` in `syncSettlementHelper.ts` — never toasts on provisional/unsettled transports, during a cert-decision flow (cert modal is the intended UX), or inside the reconnect backoff loop (only the very first failure on a settled transport may toast). Wired into `SyncConnectionContext.handleSyncError` + `connect()` catch (new `isCertFlowActiveRef`, set on `cert:verification_failed` and before TLS dials, cleared on settled connect) and `ConnectionSetupScreen.handleConnectionError` (new `showCertModalRef` mirror). Kills the "connection error / connection dropped" toast spam during pairing when TLS/cert errors are expected. **Follow-up fix (on-device logs, adb logcat 08-03): a FOURTH ungated toast path remained — `ConnectionSetupScreen.handleHandshakeAccepted`'s catch showed "Failed to establish connection" whenever `attemptSecureConnection()` rethrew. Root cause: the failing connection is `InsecureSSLWebSocketConnection` (self-signed lib) which emits plain `'error'` (not `cert:verification_failed`), and `attemptSecureConnection`'s `isCertError` string check fails on the raw RN WS event (`{_defaultPrevented:false,_type:"error",...}` → no cert keywords) → rethrow → ungated toast. Log evidence: 08-02 builds toasted BOTH "Connection error occurred" AND "Failed to establish connection"; the 08-03 build suppressed the former (gate worked) but still fired the latter. Fixed by applying the SAME gate in that catch (`isCertFlowActive = showCertModalRef.current || !hasSelectedSecurityModeRef.current` — always true during pairing since `handleHandshakeAccepted` resets the ref to false → suppressed). New scenario test in `syncSettlementHelper.test.ts` pins the exact param set.
  - **Refactor (same session): the gate wiring was copy+pasted across the 4 call sites — extracted `shouldShowConnectionErrorToastForConnection` (async, dependency-injected: `getConnectionInfo` + `getSecurityMode` getters, so `syncSettlementHelper.ts` stays pure/unit-testable) in `syncSettlementHelper.ts`, which collapses the "resolve live settled transport via `isSyncTransportSettled` + run `shouldShowConnectionErrorToast`" block; new `SyncConnectionMode` type alias extracted from the inline union. All 4 sites (SyncConnectionContext handleSyncError + connect catch, ConnectionSetupScreen handleConnectionError + handleHandshakeAccepted catch) now call it. ALSO i18n'd the toasts: ConnectionSetupScreen had 11 hardcoded English strings — converted to `t('...')` (connection namespace; keys `connectionErrorToast`, `connectionCancelled`, `securityModeFailed`, `connectionRejectedToast`, `connectionFailedToast`, `reconnectedToast`, `reconnectFailedToast`, `unpairedToast`, `connectedToast` already existed; ADDED missing `failedToEstablishConnection` key). SyncConnectionContext already used `i18n.t('syncConnection:...')` — untouched. Verification: tsc clean; unit 28 suites / 333 tests (6 new for the async helper); integration 8 suites / 31 + 1 skip.
  - CRITICAL BUG CAUGHT IN REVIEW: the estimate payload field names. Engine serializes `SyncDataSizeEstimatePayload` with snake_case JSON tags (`estimated_download_mb`, `total_records`, `image_count`, `includes_images`, `image_size_mb`) but the app (and the mock server) read/wrote PascalCase (`TotalRecords`, `ImageCount`, `EstimatedDownloadMB`) — in production the estimate would always read as 0/0/0 and the dialog would NEVER appear. Fixed app reads + mock server emission + tests (Test 11 asserts `estimate.EstimatedDownloadMB` is `undefined`).
  - Themed dialog: reordered providers in `App.tsx` — `AppAlertProvider` now WRAPS `SyncConnectionProvider` (verified safe: AppAlertModal only depends on ThemeContext) so `useAppAlert()` works in SyncConnectionContext; replaced the RN `Alert.alert` fallback with the themed obsidian-glass dialog (icon `cloud-download-outline`, `blockBackdropDismiss: true`, Cancel→reject / Confirm→accept, `showAlertRef` mirror for the `[]`-deps listener).
  - Confirmation threshold: new pure helper `shouldPromptForSyncEstimate({totalRecords, imageCount, estimatedDownloadMB, limitMB, isInitialSync})` in `src/contexts/syncEstimateHelper.ts` — prompts ONLY when `estimated_download_mb > limit` (default 5 MB), `limitMB === null` = "Unlimited" → never prompt, empty (0/0) → auto-confirm, and `isInitialSync` → ALWAYS prompt for any non-empty estimate (bypasses limit). Initial sync detected via `ConnectionStateManager.getLastSync(getCurrentSource()) === 0` (no watermark yet — the watermark is only written at SYNC_FINALIZE). Setting persisted via `ConnectionStateManager.getSyncEstimateLimitMB()/setSyncEstimateLimitMB()/clearSyncEstimateLimitMB()` under AsyncStorage key `sync_estimate_limit_mb` (default 5, `'unlimited'` sentinel). `SyncSettingsScreen` gained a "Sync confirmation limit" dropdown card (1/5/10/20/50/100/Unlimited) via the shared `SelectPicker` + i18n keys (`estimateConfirmLimit`, `estimateConfirmLimitHint`, `unlimited`).
  - Verification: app `tsc --noEmit` clean; unit 28 suites / 333 tests green (13 syncEstimateHelper + 10 ConnectionStateManager + 6 connection-toast-helper new); integration sync suite 31 pass + 1 skip (syncService 14 incl. 4 estimate tests). Changes UNCOMMITTED — staged earlier sync-race work is still uncommitted too; user to decide commit split.
- Sync-Race Fix + Sync-Watermark Wipe Reset (2026-08-02): Diagnosed and fixed the "characters not appearing after pairing" sync failure; then added full-DB-wipe watermark reset and removed the legacy sync-timestamp alias. Three work items, all verified on-device + tests green:
  - (1) Settled-transport sync gate + abort safety net — root cause: auto-sync fired on the provisional plaintext ws:// pairing connection, then the ws→wss security upgrade tore the connection down mid-sync (buffered SYNC_DATA discarded, SYNC_COMPLETE dropped 4ms after listeners removed), orphaning `SyncService.currentSession` as 'in_progress' forever → every future sync logged "Sync already in progress, skipping" until app restart. Fixes: new pure helper `isSyncTransportSettled(connMode, persistedMode)` (`src/contexts/syncSettlementHelper.ts`) — TLS/cloud always settled, `unencrypted` only if user explicitly persisted it; `SyncConnectionContext.handleSyncConnected` is now async and defers `initiateSync()` until transport settles; new `SyncService.abortSync(reason)` (clears session/phase/buffer/serverRecordIds, rejects pending confirmation, emits `sync:aborted`) wired to `disconnected:sync` / `error:sync` / new `sync:connection_replaced` event (emitted by `ConnectionManager.createConnection` BEFORE tearing down an existing sync connection — the underlying WS 'disconnected' is suppressed there); Fix D: `handleSyncComplete` now `await sendLocalChangesSequentially(...)` so the phase machine advances deterministically; `SyncSettingsScreen` resets spinner on `sync:aborted`. Buffered server data is intentionally DISCARDED on abort (last-sync not advanced → engine re-sends everything = lossless recovery). Deliberately NOT reordered `disconnectConnection` (Fix C — CRITICAL blast radius, 25 callers/17 processes; documented follow-up). Tests: `syncSettlementHelper.test.ts` (9 unit), `sync.abort.test.ts` (3 integration w/ mock ConnectionManager emitting disconnected:sync/error:sync). On-device verification (emulator-5554, `ai.soulbits.chat.dev`): logcat shows gate "deferring sync until transport settles" (23:56:49.486) + "Aborting sync session: sync connection replaced" (23:56:49.649, 23:56:53.831); no stuck state; both sessions completed cleanly (0/0 because client DB was wiped but AsyncStorage lastSync survived — engine correctly had nothing newer; NOT a regression, expected incremental behavior).
  - (2) Full-DB-wipe resets sync watermark — root cause of "no data transferred after wipe": the app's SQLite wipe only removed the legacy `last_sync_timestamp` AsyncStorage key, while the per-source keys (`last_sync_timestamp:selfhosted`/`:cloud`) that `getLastSync()`/`setLastSync()` actually use survived → client told engine "synced up to <recent ts>" → engine returned 0 records. Fix: new `ConnectionStateManager.clearAllLastSyncTimestamps()` (removes all per-source keys) called from BOTH `wipeDatabaseCompletely` (Loading screen) and `clearDatabaseData` (Dev DB viewer). Per-source get/set, unpair/re-pair, app-restart persistence all untouched — only the full-wipe flows clear watermarks.
  - (3) Legacy sync-timestamp alias removed (Option B) — no long-lived installs from pre-per-source-migration, so dropped it entirely: `getLastSync` no longer falls back to `last_sync_timestamp`; `setLastSync` no longer dual-writes; `STORAGE_KEYS.LAST_SYNC_TIMESTAMP` deleted; `SettingsScreen` + `SyncSettingsScreen` "Last sync" display now read `ConnectionStateManager.getLastSync(getCurrentSource())`. Single source of truth = per-source keys only. Tests: `connectionStateManager.lastSyncReset.test.ts` rewritten (7 tests: per-source write/read, NO legacy fallback, wipe clears both, idempotent, credentials untouched); integration `syncService.integration.test.ts` + `sync.clock.test.ts` legacy-key seeds removed, `updates last sync timestamp on completion` now asserts legacy key is NOT written.
  - Verification: unit 26 suites / 295 tests green; integration 8 suites / 24 pass + 1 skip; `tsc --noEmit` clean; GitNexus detect_changes scope = wipe flows + settings screens' last-sync display only (risk HIGH inherent to destructive wipe flows). Changes UNCOMMITTED (4 sync-race files + 3 new files + wipe/legacy edits) — awaiting user decision on commit split.
- Google OAuth Environment Secret Wiring (2026-07-31): Dev/Prod OAuth client secrets are no longer hardcoded in tracked files. New `scripts/oauth-secrets.cjs` loader reads gitignored `client_secret_*.json` (search order: --file → secrets/<env>/ → secrets/ → repo root) and derives `.env` (APP_ENV/IS_BETA/GOOGLE_WEB_CLIENT_ID/APPLE_SERVICES_ID) + `android/gradle-secrets.<env>.properties`. npm scripts `oauth:dev`/`oauth:prod`; `android`/`ios`/`start`/E2E build scripts auto-load (with `--if-missing-ok` where optional). Android `build.gradle` resolves via -P flag → gradle-secrets file → '' (gradle.properties cleaned). Runtime: `cloud.ts` exports `APP_ENVIRONMENT` (env: dev|prod + label); `react-native-config.d.ts` typed `APP_ENV`; `googleSignIn.ts` warns when webClientId missing. CI unchanged (uses vars.* with -P priority; iOS regenerates .env per matrix entry incl. APP_ENV). `.env` untracked from git. Docs: `docs/OAUTH-SECRETS.md` + INSTALLATION.md update.
- Soulbits Cloud Config Convenience Features + Character Card Import (2026-07-30): Three user-requested features centered on the module config menu + Characters screen. (1) Endpoint auto-prefill: when Soulbits Cloud is the provider AND the app is cloud-connected, `ModuleConfigEditScreen.handleProviderSwitch` prefills `base_url` with the beta-aware `CLOUD_HOSTS.inference` (overrides the prod default baked into `PROVIDER_DEFAULTS.soulbitscloud.base_url`, which was the bug — beta builds showed the prod URL). Edit-mode never overwrites a stored value. (2) Module-filtered model dropdown: new `SoulbitsModelSelect` component replaces the free-text `model` field for soulbitscloud, loading valid models for the module type via a credential-less `createClient({inferenceURL})` against the public `GET /v1/models` catalog (5-min TTL cache, single-flight, never throws → static fallback on error/offline), plus a "Custom…" free-text override. Mapping in new `src/constants/soulbitsModels.ts`; service in new `src/services/cloud/soulbitsModelsCatalog.ts`. Shared infra: `FormField` `select` case now renders a real themed `SelectPicker` dropdown (was a disabled TextInput) — fixes existing select fields (reasoning_effort, aspect_ratio) too. (3) Character card import: ported harmony-link-private `utils/charactercard/` (types.go/png_parser.go/mapper.go) to a new pure-TS package `src/utils/charactercard/` (parser handles Tavern Card V1/V2/V3 from raw JSON + PNG-embedded tEXt/iTXt `chara`/`ccv3` chunks, base64-decodes; mapper → CharacterProfile + CharacterImage via `generateId()` UUID v7). Import service `src/services/CharacterCardImportService.ts` reads a picked file (RNFS), detects PNG/JSON, parses+maps+persists via existing repos (fully local, syncs via normal pipeline). CharactersScreen gained a header "Import Card" action (`@react-native-documents/picker` `pick`). Added `base64ToUint8Array` helper to `src/database/base64.ts`. i18n keys added to `moduleConfig.json` + `characters.json`. Tests: 6 new unit suites (jsonParser/pngParser/mapper/soulbitsModels/soulbitsModelsCatalog/base64) — 48 tests, full unit suite green (269 tests). tsc clean. NOTE: pre-existing eslint errors in ModuleConfigEditScreen (useCallback exhaustive-deps, unused moduleSpecificDefaults) + CharactersScreen (unused `Nav` type) are PRE-EXISTING (verified via git HEAD) — out of scope, left untouched. The `no-bitwise` warnings in the new binary-parsing code are inherent and don't fail CI. Manual/on-device verification deferred to user (build not run per workspace rules).
- Cloud-Mode Token Handling + Soulbits Model Defaults + TLS/UX fixes (2026-07-31): Six commits landed this session.
  - `353bf97 fix: Connection issue handling` — ConnectionSetupScreen cert-flow hardening: `hasSelectedSecurityModeRef` reset in `handleHandshakeAccepted`/`handleCredentialsCleared`/unpair/modal-abort; `handleCertVerificationFailed` → alert offering security-mode reset (i18n certResetTitle/Message/Confirm) then opens cert modal; "Reset Security Mode" button clears mode+resets UI+toast+opens cert popup; removed `setSecurityMode('secure')` dead-end. Root cause of phone TLS failure: app stores `harmony_server_cert` but never pins it; engine cert self-signed, SANs only localhost/127.0.0.1/::1 (no LAN IP) → "unknown certificate". Fix = re-pair in Insecure-SSL mode. Engine restarted with `SE_WSS_PORT=28443` (WSS/TLS); plain WS 28080, admin 28081.
  - `3167a8e feat: add speed dial button` — CharactersScreen FAB + modal replaced by RN-`Animated` speed dial: FAB rotates into ✕, two floating glass pills above it (Create Profile `testID=create-profile-option`, Import Character Card (PNG / JSON) `testID=import-character-card`), staggered open 220ms / close 180ms, backdrop/✕ closes; removed old modal + `createOrImport` i18n keys.
  - `7b3eb74 feat: simple profile picker` — CreateAIScreen links an EXISTING character profile instead of always creating a fresh one: profile selector + bottom sheet ("Create new profile" + all profiles); choosing one links the new entity to it (no unique constraint on `entities.character_profile_id` — verified valid), prefills Name/Personality, shows preview card with pencil → CharacterProfileEdit, hides photo/personality fields; alias conflict (`idx_entities_alias_unique`) → "Name already in use" alert instead of crash; `prefillProfileId` route param now honored.
  - `91fe902 fix: module pickers on create AI screen` — Advanced Settings: pickers were gated behind `hasAnyConfigs===true` (local-DB check) → empty DB showed dead-end "Connect to Harmony Link or Cloud…" warning. Deleted inline `ModuleConfigPicker` + warning block (~310 lines); 8 pickers now use shared `EntityModuleSelectorWithActions` (selector + ✎ edit + ＋ create → ModuleConfigEdit), always visible; `loadModuleConfigs` stable `useCallback`; `useFocusEffect` reload on return.
  - `5a10a51 fix: soulbits models defaults` — **no "soulchat" model exists on the live catalog** (`GET https://beta.api.soulbits.app/v1/models`, 20 models). Fake IDs removed: `soulchat-v1` (LLM fallback + `moduleDefaults.ts` default + `providerFieldSchemas.ts` placeholder), `soul-embed-v1`/`soul-tts-v1` (fallbacks), `whisper` (stt). Real replacements: LLM `qwen-35-9b`/`starfallen-24b`/`gemma4-meromero-26b-a4b`, embeddings `harrier-oss-v1-0-6b`/`qwen3-embed-4b`, TTS `harmonyspeech`/`kitten-tts-*`/`openvoice_*`/`chatterbox*`, STT `faster-whisper-tiny`/`faster-whisper-large-v3-turbo`, vision `qwen-35-9b`/`gemma4-meromero-26b-a4b`; default model `qwen-35-9b`. Added guard test: every fallback ID must exist in live catalog.
  - `5d5e7fe feat: convenience features for Cloud mode token handling` — see dedicated @Focus entry below.
- Cloud-Mode Token Handling design + implementation (2026-07-31): Engine (harmony-link-private) already injects the session PASETO into its seeded soulbitscloud row (`cmd/cloud_mode.go seedCloudSoulbitsCloudProvider`: seeds "Soulbits Cloud (default)" with `api_key=SE_INFERENCE_TOKEN`; `providers/soulbitscloud.go` switches PASETO-mode iff `api_key` starts with `v4.local.`). App-side gap closed: (a) **Inject on create** — `ModuleConfigEditScreen.saveProviderConfig` calls `injectSoulbitsToken({providerType, isCreate:!form.providerConfigId, providerConfig})` → sets `api_key` to current PASETO for NEW soulbitscloud configs only (updates/non-soulbits/standalone untouched); `handleProviderSwitch` also prefills `api_key` from `AuthService.getToken()`. (b) **Refresh propagation** — new `src/services/cloud/soulbitsTokenSync.ts`: `startSoulbitsTokenSync()` subscribes to `AuthService.on('auth:changed')` (login+register+refresh), bulk-updates all non-deleted soulbitscloud rows via new `updateAllSoulbitsCloudApiKeys(paseto)` repo fn (bumps `updated_at` so sync picks it up), THEN calls `SyncService.initiateSync()` to push to engine immediately (self-guards on in-progress/disconnected; connect-time auto-sync is the safety net). `syncCurrentToken()` one-shot on startup; `injectSoulbitsToken()` shared by UI. Wired in `AuthContext` useEffect (below DatabaseProvider → DB ready). (c) **Read-only credential UX** — `FormField` gained `readOnly?: boolean`: for password fields `editable={false}` + `secureTextEntry` locked + NO eye toggle + `contextMenuHidden`+`selectTextOnFocus={false}` (can't copy/reveal/break); soulbitscloud api_key rendered read-only when `isManagedSoulbitsApiKey` (same `v4.local.` prefix heuristic as engine). Tests: repo bulk-update (active rows updated, soft-deleted preserved), service (subscribe/unsubscribe, sync trigger, inject rules) — all green 279 tests/24 suites; tsc clean; GitNexus LOW on FormField (additive prop), medium detect_changes all internal to ModuleConfigEditScreen.
- Dev-mode detection audit + beta build commands (2026-07-31, informational): App knows dev via Metro `__DEV__` (compile-time) — gates logger severity, DatabaseTableViewer screen/menu/settings card, and `cloud.ts` `IS_BETA = __DEV__ ? true : Config?.IS_BETA`. FOOTGUN: exported `IS_DEV` alias = `IS_BETA` (beta-flavour), NOT `__DEV__`. soulbitscloud base_url prefill only when `cloudConnected` (mode==='cloud' && connected) — in dev connected to the local engine, creating a soulbitscloud config prefills PROD `api.soulbits.app`, while the model catalog (`soulbitsModelsCatalog.ts`) unconditionally hits `CLOUD_HOSTS.inference` (= beta in dev). Beta release build: `cd android; .\gradlew.bat assembleDevRelease -PE2E_DEBUG_SIGNING=true` (debug-signed, JS-bundled; only debug.keystore exists locally — no release keystore); output `android\app\build\outputs\apk\dev\release\app-dev-release.apk`; install `adb -s d608825c install -r <apk>`. `npm run android-prod` = prod flavor (IS_BETA=false) — NOT beta.
- Soulbits Cloud Config Convenience Features + Character Card Import (2026-07-30): Three user-requested features centered on the module config menu + Characters screen. (1) Endpoint auto-prefill: when Soulbits Cloud is the provider AND the app is cloud-connected, `ModuleConfigEditScreen.handleProviderSwitch` prefills `base_url` with the beta-aware `CLOUD_HOSTS.inference` (overrides the prod default baked into `PROVIDER_DEFAULTS.soulbitscloud.base_url`, which was the bug — beta builds showed the prod URL). Edit-mode never overwrites a stored value. (2) Module-filtered model dropdown: new `SoulbitsModelSelect` component replaces the free-text `model` field for soulbitscloud, loading valid models for the module type via a credential-less `createClient({inferenceURL})` against the public `GET /v1/models` catalog (5-min TTL cache, single-flight, never throws → static fallback on error/offline), plus a "Custom…" free-text override. Mapping in new `src/constants/soulbitsModels.ts`; service in new `src/services/cloud/soulbitsModelsCatalog.ts`. Shared infra: `FormField` `select` case now renders a real themed `SelectPicker` dropdown (was a disabled TextInput) — fixes existing select fields (reasoning_effort, aspect_ratio) too. (3) Character card import: ported harmony-link-private `utils/charactercard/` (types.go/png_parser.go/mapper.go) to a new pure-TS package `src/utils/charactercard/` (parser handles Tavern Card V1/V2/V3 from raw JSON + PNG-embedded tEXt/iTXt `chara`/`ccv3` chunks, base64-decodes; mapper → CharacterProfile + CharacterImage via `generateId()` UUID v7). Import service `src/services/CharacterCardImportService.ts` reads a picked file (RNFS), detects PNG/JSON, parses+maps+persists via existing repos (fully local, syncs via normal pipeline). CharactersScreen gained a header "Import Card" action (`@react-native-documents/picker` `pick`). Added `base64ToUint8Array` helper to `src/database/base64.ts`. i18n keys added to `moduleConfig.json` + `characters.json`. Tests: 6 new unit suites (jsonParser/pngParser/mapper/soulbitsModels/soulbitsModelsCatalog/base64) — 48 tests, full unit suite green (269 tests). tsc clean. NOTE: pre-existing eslint errors in ModuleConfigEditScreen (useCallback exhaustive-deps, unused moduleSpecificDefaults) + CharactersScreen (unused `Nav` type) are PRE-EXISTING (verified via git HEAD) — out of scope, left untouched. The `no-bitwise` warnings in the new binary-parsing code are inherent and don't fail CI. Manual/on-device verification deferred to user (build not run per workspace rules).
- Issue-2 Cloud Sync Rejection + UX Feedback (2026-07-30): Cloud-mode SYNC_REQUEST was rejected by HL because the device was never registered — the app's cloud connection intentionally skips HANDSHAKE_REQUEST (Cloud-Connection-Redesign Phase 4 "handshake trim"; PASETO at conduct-proxy replaces the HL handshake JWT), so the registration side-effect stranded in the handshake path never ran. Backend Fix #1 lives in harmony-link-private (extracted `findOrCreateDevice` shared by both handlers; handleSyncRequest now auto-registers+approves in cloud mode). APP-SIDE FIXES (this repo): added the missing `sync:rejected` consumers. The `sync:rejected` event was already emitted by SyncService (SYNC_REJECT→emit) and declared in `SyncServiceEvents` — only the UI listeners were missing, so a reject left `isSyncing=true` forever (initiateSync is fire-and-forget: it only awaits `sendEvent`, and no SYNC_ACCEPT/sync:error arrives after a reject). (1) SyncSettingsScreen.tsx: added `rejectedListener` → `setIsSyncing(false)` + `setCurrentSession(null)` + toast `syncSettings:syncRejected` (fallback to `syncError`); registered in the existing effect with matching `removeListener` cleanup. (2) SyncConnectionContext.tsx: added `handleSyncRejected` toast as defense-in-depth from any screen (e.g. auto-sync on connect); registered/cleaned alongside the existing `sync:completed`/`sync:error` handlers (`SyncService.off`). (3) i18n: added `syncRejected` key to BOTH `syncSettings` + `syncConnection` namespaces (en/ only — single locale dir). E2E regression guard `TestSyncRequestApproval` (soulbits-cloud-backend tests/e2e) sends a bare SYNC_REQUEST and asserts SYNC_ACCEPT. Build/test deferred to user.
- Soulbits API Client — dependency source fixed (2026-07-30): Client is now consumed as a real GitHub git dependency, NOT a local copy. Previously `package.json` declared `"file:../soulbits-api-client-js"` + an UNTRACKED `.npmrc` with `install-links=true` (forced a local-folder copy into node_modules; never touched GitHub). Now: `package.json` → `"git+ssh://git@github.com/harmony-ai-solutions/soulbits-api-client-js.git#518c9e8e9f36c9278489b861b3bed07d452ba3bc"`; `.npmrc` deleted; `package-lock.json` resolves git URL + commit + integrity hash. Required a `prepare` script on the CLIENT repo (`prepare: npm run build` → tsup) so npm builds the gitignored `dist/` on git-install (client only had `prepublishOnly`, which npm does NOT run on git installs — that's why dist was missing on first try). Client commit `518c9e8` (pushed) carries the prepare script. On a client bump: commit+push client, then re-pin the app's commit SHA + `npm install`. No npm publish needed.
- Soulbits API Client Adoption (2026-07-27): `CloudSessionService` now drives the session broker through the first-party JS client (`@harmony-ai-solutions/soulbits-api-client`, now a GitHub git dependency (see the 2026-07-30 note above)) instead of hand-rolled `authFetch`. New `src/services/cloud/soulbitsClient.ts` factory `buildSoulbitsClient({paseto})` builds the client **PASETO-only** (no `refreshToken`) → the client injects `Authorization: Bearer` but NEVER auto-refreshes on 401; a 401 surfaces to `CloudSessionService._doConnect`, which refreshes via `AuthService` (mirrors `AuthService.fetch` single-refresh-retry + `invalidate()`/`AuthExpiredError` on terminal). This keeps ONE persisted/broadcast credential for both REST and WebSocket paths (the client refreshes in-memory only with no read-back hook → would desync Keychain/WS). The app keeps its OWN poll loop using the client's `session.connect()` primitive — NOT `connectPoll` — to preserve the `requesting→provisioning→ready/failed` state machine, the per-poll UI feedback (`CloudProvisioningCard` elapsed timer), and the mid-poll `_cancelled` cancellation contract. `session.disconnect(sid)` replaces the disconnect RPC. `config/cloud.ts`: added `CLOUD_HOSTS.session` (= session-broker host, used as the client `cloudURL`); removed dead `AUTH_ENDPOINTS.sessionConnect/sessionDisconnect`. `authFetch.ts` is now ORPHANED (sole consumer was CloudSessionService) — left in place as a documented public-API wrapper. **Public API of CloudSessionService unchanged** → no consumer edits (`connect`/`disconnect`/`getStatus`/`getSessionId`/`getReadyAt` + `status` event + `CloudSessionStatus`/`CloudSessionInfo` types all preserved). Tests: `__tests__/cloudSessionService.test.ts` mocks `buildSoulbitsClient` + AuthService; 14/14 pass (added 401-refresh + disconnect-via-client cases). Stale GitNexus index (101 commits behind) — `detect_changes` listed 4 changed files (correct) but mapped symbols to `SyncConnectionContext` (a file never touched) — ignore as staleness artifact.
- Cloud Session Async Provisioning (Phase 11, complete 2026-07-25): Cloud session connection is now a fully async polling flow. `CloudSessionService` is an async state machine with states `idle → requesting → provisioning → ready → failed`. No `active` state at the service level — WS connectivity tracked by `useSyncConnection().isConnected`. The polling loop sends `POST /v1/session/connect` at the broker's `retry_after_ms` (500ms floor, 95-poll cap ≈190s) until the session is `ready` (200) or `failed` (503). The `BROKER_CONNECT_TIMEOUT_MS` client-side hack is removed — stale-session detection replaced by a consecutive-WS-failure counter (5 → fresh re-provision) and the broker's own lifecycle timers. A token-expiry pre-check runs before every cloud-mode WS dial. `canUseChat` gates chat/chat-list UI: cloud→`status==='ready'`, self-hosted→`isPaired`. `connectionStatus` produces mode-aware display labels. ConnectionSetupScreen shows a multi-stage provisioning card (Requesting → Preparing → Establishing → Connected ✓ / failed+retry) with elapsed timer and reduced-motion a11y. Coupled release: backend + app deploy together (no API versioning). See `docs/CLOUD-CONNECTION.md` for full detail.
- Test Flakeness Root Cause + Workaround (2026-07-24): unit DB suites (NodeDatabase / better-sqlite3 12.11.1 on Node 25) intermittently fail "Received function did not throw" on UNIQUE/FK/transaction-rollback when 2+ DB test files share a Jest worker — native-addon GC/timing contamination across :memory: DBs in one worker process. Deterministic under --detectOpenHandles (forces serial), intermittent in default parallel (bit 1-in-~6 observed runs). Extensive bisection RULED OUT: :memory: sharing, global UNIQUE disable, SQL-text/statement-cache collision, the transaction impl — failure is GC/native-timing at a file boundary, only reproducible with the real files (identical synthetic probe passes). FIX = isolation workaround, not a code cure: package.json `test:unit` = `jest --selectProjects unit --maxWorkers=16` (>= 12 unit files → 1 file/worker → no sharing → no contamination, verified 3/3 stable); `npm test` now = `test:unit && test:integration`; `test:raw` preserves old `jest`. ALSO fixed NodeDatabase to NOT apply PRAGMA journal_mode=WAL to :memory: (WAL is meaningless for in-memory DBs and better-sqlite3 HANGS outright on WAL+:memory: — reproduced standalone). If unit file count grows >16, bump --maxWorkers in package.json. Full analysis: docs/TESTING.md#known-issue-better-sqlite3-cross-worker-contamination.
- Project upgraded from React Native 0.83.1 → 0.86.0 and React 19.2.0 → 19.2.7 (2026-07-19). Required due to codegen incompatibility: `react-native-screens` v4.26.2 uses `React.ComponentRef<>` but RN 0.83.1's codegen parser only accepts `React.ElementRef<>`. Companion packages bumped to 0.86.0. The Gradle config (compileSdk 36, buildTools 36.0.0, Kotlin 2.1.20, NDK 27.1.12297006) was already compatible. Resolved a build-tasks ambiguity from product flavors (`dev`/`prod`) by adding `--mode=devDebug` to the `npm run android` script.
- Migration SQL Safety Guard + Stale-Column Investigation (2026-07-19): Investigated reported stale columns from migrations 007 (`audio_file` on chat_messages/conversation_messages) and 021 (`chat_template_kwargs` on openai/openaicompatible/openrouter provider configs). FINDING: both were already removed by LATER migrations — `audio_file` was dropped incidentally by migration 013's conversation_messages rebuild (its column list omitted it alongside the intended `vl_model_embedding`); `chat_template_kwargs` was dropped by the rebuilds in 030 (openai/openaicompatible) and 031 (openrouter). No cleanup migration was needed; the FIXME comments in 007/021 are stale leftovers. Added a forward-looking guard to the migration runner (`src/database/migrations.ts`, `assertMigrationSqlSupported` called at the top of `applyMigration`): migrations now FAIL FAST at apply time if executable SQL contains `ALTER TABLE ... DROP COLUMN` or `RENAME COLUMN` (unsupported on older-Android SQLite, breaks schema parity). Comment-stripping (`--` line + `/* */` block) ignores the FIXME documentation lines. Verified against all 32 existing app migrations (none trip; `DROP TABLE` / `RENAME TO` correctly allowed). Equivalent guard mirrored into harmony-link-private (Go); HL migrations 025 & 027 converted from DROP COLUMN to the `_new`-table rebuild pattern preserving HL's BLOB/TIMESTAMP/BOOLEAN types so the HL guard is unconditional.
- Universal Glassmorphism Overhaul Complete: All app cards now use Obsidian Glass — 45-55% opacity translucent fill with specular gradient borders and ambient neon glow shadows. GlassTokens extended with glowOpacity+glowRadius. ThemedCard is the single source of truth; 9 themes updated; ThemeCard and LandingCard use matching glass styling.
- Cloud Auth Layer (Phase 5) Complete: First HTTP layer + cloud auth for soulbits-cloud-backend. `src/config/cloud.ts` (CLOUD_HOSTS, WS_PATHS, OAUTH, AUTH_ENDPOINTS); `src/services/auth/` (tokenStorage Keychain `com.harmonyai.cloud.auth`, authFetch thin wrapper, AuthService singleton with fetch()/401-refresh-retry, login/register/loginWithGoogle/loginWithApple/refresh/logout/getProfile/getToken/getTokenExpiresAt/invalidate/resendVerification); `src/contexts/AuthContext.tsx` (status: loading|authenticated|unauthenticated). Provider wired between Database and SyncConnection. `getTokenExpiresAt()` from backend `expires_at` (NOT PASETO decode).
- Cloud Connection + Provider Mirror (Phases 6-7) Complete: harmony-ai-app now connects to Soulbits Cloud.
  - Phase 6 (cloud connection + session broker): `src/services/websocket/CloudWebSocketConnection.ts` (4th `ConnectionMode='cloud'`; RN built-in WS, standard TLS, PASETO via `Sec-WebSocket-Protocol: Bearer.<paseto>` against conduct proxy `/ws/sync`+`/ws/worker`; cert-error block dropped — ACM cert); `src/services/cloud/CloudSessionService.ts` (broker-only: POST /v1/session/connect spawn/claim + /disconnect grace; does NOT open WS itself); per-source lastSync (`ConnectionStateManager.SyncSource` + `getCurrentSource()` reads `connection_mode` AsyncStorage key); mode-branching in SyncConnectionContext (`/ws/sync`) + EntitySessionService (`/ws/worker`, 2 spots); PASETO-expiry handling — proactive timer (`CloudSessionService.scheduleProactiveRefresh` = `getTokenExpiresAt()-10min`, reschedule on refresh) + reactive (`CloudWebSocketConnection` onclose 1008/4401 → `AuthService.refresh()` → fail→`invalidate()`→AuthContext login).
  - Lifecycle wiring: AuthContext calls `cloudSessionService.connect()` on bootstrap/auth:changed IF `connection_mode==='cloud'`; `cloudSessionService.disconnect()` BEFORE `AuthService.logout()` (in AuthContext, NOT AuthService — avoids circular dep AuthService→cloudSessionService→authFetch→AuthService). EntitySessionService background AppState → `cloudSessionService.disconnect()`. Switch-warning Alert + spawning-spinner in ConnectionSetupScreen (`cloudStatus` state subscribes to cloudSessionService 'status').
  - Phase 7 (provider mirror): 7-0a split monolithic `providers.ts` (~3080 lines) into 16 per-file `<X>ProviderConfigRepository.ts` + `shared.ts` under `repositories/providers/` (NO shim; monolith deleted; all 4 importers updated). 7-0 migrated config-table family INTEGER→UUID PK (migration 031, mirrors HL 3-0) — full 7-tier cascade (models interfaces, 15 provider repos `id:string`+`createX` pre-gens `generateId()` UUID v7 via new `src/utils/uuid.ts`, modules.ts, entities.ts, SyncService/sync verify no Number() coercion, UI parseInt sites fixed in CreateAIScreen/EntityConfigEditScreen/EntityModuleSelector/AppNavigator). 4-way reconciliation per table. 7-1 added soulbitscloud (migration 032 UUID-native, `SoulbitsCloudProviderConfigRepository.ts`, model interface + ProviderConfig union member, providerFieldSchemas entry, SyncService 3 dispatch sites — lands the 6-3-deferred sync-dispatch).
  - Credential separation unchanged: cloud PASETO in Keychain `com.harmonyai.cloud.auth`; self-hosted `harmony_jwt` in AsyncStorage. `getTokenExpiresAt()` from backend `expires_at` (NOT PASETO decode — v4.local encrypted).
- Login UIs: `screens/auth/{LoginScreen,RegisterScreen,VerifyPrompt}` + mode toggle in `ConnectionSetupScreen` (self-hosted/cloud, `harmony_mode` persisted). New `auth` i18n namespace registered in `I18nContext`.
- Social Login: Google (`@react-native-google-signin/google-signin`, typed error discriminator, non-GMS fallback) + Apple (`@invertase/react-native-apple-authentication`, iOS-only AppleButton). Both handle 409 email-conflict messaging.
- Phase 8 (build flavors + CI) Complete: react-native-config@1.6.1 wired into src/config/cloud.ts (replaced ambient stub); IS_BETA centralized coercion (=== true || === 'true') — Android bridge surfaces JS boolean via BuildConfig reflection; iOS surfaces NSString from .env so cloud.ts coerces. Android productFlavors dev/prod (android/app/build.gradle: buildConfigField "boolean" IS_BETA, dev applicationIdSuffix ".dev", resValue build_config_package, debuggableVariants=["devDebug"], google-services plugin 4.4.2 + placeholder google-services.json both package names). iOS dev/prod is CI-driven (.github/workflows/build-release.yml dev/prod matrix: regenerates .env per entry + PRODUCT_BUNDLE_IDENTIFIER override; single scheme, NO pbxproj/scheme surgery).
- Circular-dep lesson: 401-refresh-retry logic lives on AuthService.fetch(); authFetch.ts is a thin one-way wrapper (authFetch→AuthService→tokenStorage) — NO dynamic import.
- Credential separation: cloud PASETO in Keychain (`com.harmonyai.cloud.auth`); self-hosted `harmony_jwt` in AsyncStorage untouched.
- Provider Expansion Complete: xAI/Google/Anthropic provider support added across all module types. Model field unification (migration 000030). Dead code cleanup. 15 provider schemas in providerFieldSchemas.ts. 21 provider CRUD functions in providers.ts. ModuleConfigEditScreen supports all 15 providers. Module defaults include Google/xAI/Anthropic (maxtokens: 200).
- Documentation Complete: Comprehensive Harmony Link integration documentation created in /docs/HARMONY-LINK-INTEGRATION.md with detailed diagrams in /docs/harmony-link-diagrams/.
- Chat Feature Implementation Complete: Full chat interface with text/audio support, tap-based voice recording, persistent storage, real-time event handling.
- Database Layer Complete: SQLite with TEXT columns for base64-encoded audio/image data, chat_messages schema with audio/image support. Migrations in /src/database/migrations/.
- Repository Layer Complete: chat_messages.ts repository with CRUD operations, bidirectional conversation queries, pagination support.
- Audio Services Complete: AudioRecorder (tap-based with permission handling), AudioPlayer for playback.
- Voice Features Complete: STT (Speech-to-Text) and TTS (Text-to-Speech) working with Harmony Link integration.
- Native Dependencies: react-native-audio-record, react-native-track-player, react-native-fs, react-native-image-picker.
- iOS/Android Setup: Microphone permissions, storage permissions, runtime permission checks.
- Event System Complete: EntitySessionService with dual session management (user + partner entities), typing/recording indicators, CONNECTION_PING/PONG heartbeat.
- UI Screens Complete: ChatListScreen (conversation overview with entity selection), ChatDetailScreen (messaging with QoL features).
- UI Components Complete: ChatBubble (text/audio/image with context menu), ChatInput (tap-based voice recording), TypingIndicator, NewMessagesDivider.
- Navigation Complete: ChatDetail route with impersonatedEntityId param for roleplay support.
- Multi-Connection WebSocket Architecture Complete: ConnectionManager supporting multiple concurrent connections (sync + entity sessions), independent lifecycles.
- Sync Services Complete: EventEmitter3-based events, three security modes (secure/insecure-ssl/unencrypted), handshake-first pairing protocol.
- Device Pairing Protocol: ws:// handshake → user approval → wss:// upgrade → cert validation → security mode selection.
- Database Complete: SQLCipher encryption, full repository layer (84+ tests), in-app DEV-only test runner, sync tables.
- Test Runner: DatabaseTestScreen in Settings menu, real-time console, color-coded results, themed UI.
- Advanced Theming Complete: RGB editor, custom theme creation/deletion, JSON import/export, 6 built-in themes.
- Logging Infrastructure: react-native-logs with production-ready levels, colored dev output, unified [Namespace] prefixes.
- Bootstrap Complete: RN 0.83.1, TypeScript, navigation, Paper UI, organized structure.
- Safe Area Inserts Applied: Android system navigation bar safe area bottom padding across all screens (ChatInput, CharacterProfileEdit, Characters, ChatList, CreateAI, EntityConfigEdit, EntityConfig, Landing, Settings, ModuleConfigEdit).
- Session Resume Implemented: Sessions auto-resume on disconnect; dangling events reconciled and synced to clients on reconnect.
- Reply Mode Feature: Per-chat-partner instant vs. realistic reply time toggle, persisted via AsyncStorage.
- Emoji Features Complete: Full emoji picker (native/Twemoji/Noto sets), EmojiAction system with Ekman8 emotion effects, entity-level per-emoji action customization, DB migrations 22-23, emoji_actions repository.
- Interactions Data Layer Complete: Interaction model, repository (CRUD, participant key derivation, scope derivation), migrations 24-28 (interactions table, drop session_id, interaction summary, memory lifecycle fields, presence_type).
- Session Management Refactored: InteractionSession replaces DualEntitySession — sessions are interaction-scoped (not entity-pair-scoped), events keyed by interactionId.
- Chat Screens Adapted: ChatListScreen and ChatDetailScreen adapted to InteractionSession model; JOIN-based queries for chat list previews (conversation_messages JOIN interactions).
- Force Full DB Sync: Option to force full database synchronization at the sync settings screen.
- Dynamic Atmospheric Background: Aurora/Nebula animated background with stardust particles integrated as a persistent layer behind the navigation stack. Uses Haute Goth palette (Deep Goth Blue-Black, Neon Magenta, Indigo) with 5 animated gradient blobs, prime-number cycle durations, and 40 drifting light particles with twinkling opacity.
@Next:
- On-device verification (user): rebuild Android + confirm the Block Users feature: open another user's public profile → tap the **⋯** next to Follow → Block (confirm dialog) → their AI characters, posts and notifications disappear from Discover/Characters/Market/Notifications; unblock from **Settings → Account & Security → Blocked Users** → content returns.
- On-device verification (user, 2026-08-15): rebuild Android + confirm the multi-bubble stack: open bubbles for 4 different characters → all 4 avatars show in one draggable stack (each with its own unread badge); drag moves the whole stack; tapping each bubble opens that character's chat window; tapping a different bubble switches the window; long-press + drag a bubble to the ✕ removes only it; opening a 5th character replaces the oldest bubble; reopening an existing character now updates its avatar (the original stale-avatar bug).
- On-device verification (user, 2026-08-15): rebuild Android + confirm the floating chat window loads when the bubble is tapped while the app is backgrounded (leave app → tap bubble → overlay loads → chat appears). Also confirm no regression: opening the bubble inside the app still works, closing the window restores the paused lifecycle (battery-friendly), and returning to the app with the window open then closing it doesn't freeze the app.
- Commit decision (user): the name-clash resolution feature + keep ID-cascade fixes (syncNameClash.ts, SyncService.ts, migration 000034, AppAlertContext guard, test suites) are UNCOMMITTED, as is the prior sync QoL work (estimate confirmation + toast suppression + threshold) and sync-race/wipe-reset work, and the updated_at-bump config-update fixes. Engine repo (harmony-link-private) has its own uncommitted companion changes (migration 000034, seeder guard, LWW tie-break, lifecycle provider-selector). Propose a commit split for review.
- Re-test on device after next rebuild: name-clash keep now re-sends the adopted module config + renamed provider config with server ids (user confirmed working on the current build 08-04). The stt_configs cascade regression is covered by integration tests; a fresh device sync with an stt_configs clash would confirm end-to-end.
- On-device verification (user): build beta APK (`.\gradlew.bat assembleDevRelease -PE2E_DEBUG_SIGNING=true` in android/), install (`adb -s d608825c install -r`), confirm: size-estimate dialog appears on initial sync, respects the threshold dropdown (incl. the new 1 MB option) on subsequent syncs, no "connection error" toast spam during pairing/cert flow. First verification pass (08-03) caught the 4th ungated toast — fixed, re-verify on next build.
- Release signing (owner): no release keystore exists locally — generate one (keytool) + wire RELEASE_KEYSTORE_* gradle props when a distributable beta is needed (drop -PE2E_DEBUG_SIGNING).
- Vision Module Integration: Complete integration with Harmony Link vision module for image understanding and multimodal interactions.
- Imagination Module Integration: Complete integration with Harmony Link imagination module for autonomous entity actions and initiative behaviors.
- Emoji Action Backend Pipeline: Complete send-pipeline integration of emoji action AdditionalEffects payload with Harmony Link backend.
- Image Messages: Complete implementation of image sending/receiving in chat (UI not fully complete).
- Integration Testing: Test end-to-end chat flows with live Harmony Link backend.
- Sync Protocol Testing: Validate SYNC_REQUEST → SYNC_DATA → SYNC_COMPLETE → FINALIZE flows.
- Character Management: Enhanced profiles, avatar caching, character switching improvements.
- On-Device AI: React Native Executorch integration for offline chat.
- Phase 8 PENDING (owner): set GOOGLE_WEB_CLIENT_ID/APPLE_SERVICES_ID GitHub Variables + replace placeholder google-services.json (both package names + SHA-1s); first CI dispatch run; SIWA entitlement deferred to signed-build follow-up.
- `IS_DEV` alias footgun: `IS_DEV === IS_BETA` (beta flavour), NOT `__DEV__` — audit any importers before use.
- soulbitscloud base_url prefill gap: prefills prod `api.soulbits.app` when NOT cloud-connected (e.g. dev build on local engine) while model catalog hits beta — decide if the prod default in `PROVIDER_DEFAULTS.soulbitscloud.base_url` should become beta-aware.
@Decisions:
- Chat Message Types: Support 'text', 'audio', 'combined' (image support in progress).
- Audio Storage: Base64-encoded strings stored in TEXT columns (no BLOB/Uint8Array), transmitted directly via WebSocket.
- Audio Recording UI: Tap-to-start/stop (not hold-to-record) for better mobile UX.
- Dual Session Pattern: Each chat requires TWO entity sessions (user + partner) for separate event streams - critical architecture pattern.
- Multi-Connection Architecture: ConnectionManager handles multiple concurrent WebSocket connections with independent lifecycles.
- Device Pairing Flow: Handshake FIRST (unencrypted ws://) → user approval → upgrade to wss:// → cert validation → security mode selection.
- Security Modes: Three options (secure with validation, insecure-ssl without validation, unencrypted ws://).
- Heartbeat Protocol: Application-level CONNECTION_PING/PONG every 30s to maintain session.
- Real-time Indicators: Typing and recording indicators provide better UX.
- Optimistic UI: Store messages locally first, sync in background, handle conflicts.
- React Native CLI (not Expo) for full native module control.
- React Native Paper for Material Design 3 consistency.
- React Navigation for type-safe routing.
- TypeScript strict mode for type safety.
- Hooks -> Redux/Zustand as complexity grows.
- Dark theme default (matches Harmony Link, OLED-friendly).
- Database schema 100% compatible with Harmony Link for bidirectional sync.
- DEV-only test runner using __DEV__ checks for production safety.
- Logging: react-native-logs for all non-test code; console for test runners.
- InteractionSession Pattern: Replaces DualEntitySession; sessions scoped to interaction IDs (not partner entity pairs); events keyed by interactionId.
- Reply Mode: Per-chat-partner AsyncStorage preference; defaults to "realistic" (simulated typing delay), toggle to "instant".
- Emoji Action System: Entity-level emoji → action mappings persisted in DB (emoji_actions table), synced via WebSocket pipeline.
- Emoji Sets: Supports native (platform default), Twemoji (Twitter), and Noto (Google) sprite sheets via EmojiContext.
- Ekman8 Emotion Model: Emoji actions map to 8-base emotion deltas (joy, sadness, trust, disgust, fear, anger, surprise, anticipation), sent as additional_effects with messages.
@Decisions:
- Audio Duration Storage: audio_duration persisted to DB at message creation time (EntitySessionService for TTS messages) and on-load enrichment (ChatDetailScreen for historical messages). ChatBubble has zero DB writes.
- AudioPlayer Singleton Pattern: AudioPlayer is a singleton instance; static methods (e.g., getDurationFromBase64) must be called on the class itself (AudioPlayer as AudioPlayerClass), not the singleton default export.
- music-metadata v11: Replaces deprecated music-metadata-browser; works in Metro/RN bundler environments; parseBuffer(buffer, { mimeType }) returns format.duration in seconds.
@Decisions:
- CloudSessionStatus states: idle|requesting|provisioning|ready|failed. NO `active` service-level state — WS connectivity is tracked by useSyncConnection().isConnected. `canUseChat` (cloud→ready, self-hosted→isPaired) gates chat list and settings. `connectionStatus` produces mode-aware labels.
- BROKER_CONNECT_TIMEOUT_MS removed: the 30s client-side "force re-provision on stale ready" hack is superseded by the consecutive-WS-failure counter (5 → re-provision) and the broker's own lifecycle timers (connect-timeout arms at `ready`, abandoned-ready sweeper).
- Consecutive WS failure counter (MAX_WS_FAILURES_BEFORE_REPROVISION = 5): when RN WebSocket upgrades fail repeatedly without close codes, the app re-provisions the broker session instead of retrying against a dead session.
- Token-expiry pre-check before every cloud-mode WS dial: prevents expired-PASETO reconnect loops.
- Coupled release: backend + app deploy together. POST /connect response contract changes (409→202, 200-active→202-provisioning, failed→503). No API versioning; Beta project patches breaking changes through.
@Learnings:
- ReactHost-lifecycle lesson (floating overlay stuck spinner): a `ReactSurface` created via `ReactHost.createSurface()` shares the host's ONE JS runtime + lifecycle with the main activity. When the activity backgrounds, `ReactHostImpl.onHostPause()` → `ReactContext.onHostPause()` → `JavaTimerManager.onHostPause()` sets `isPaused=true` and REMOVES the Choreographer frame callback — ALL `setTimeout`/`setInterval` in JS freeze until `onHostResume()`. Any second React root that depends on timers (e.g. a DB-ready `setInterval` poll) will mount its initial frame but never finish loading while the activity is paused. Fix: force `host.onHostResume(activity)` (1-arg — does NOT overwrite `DefaultHardwareBackBtnHandler`) before starting the surface, and restore `host.onHostPause()` (no-arg — no activity-identity assertion) when the surface stops, guarding both with the real app-foreground state so a foregrounded host is never paused. The `appStateDidChange:'active'` JS emission from the forced resume is harmless (consumers here only react to 'background').
- Wire-format-mismatch lesson: a payload consumer reading PascalCase keys against a snake_case JSON producer fails SILENTLY (all fields read as 0/undefined) — the feature looks broken-or-missing and mock servers that mirror the wrong casing hide it in tests. When an app reads engine payloads, verify the producer's actual JSON tags (Go `json:"..."` tags) FIRST, and assert the wrong-case key is `undefined` in tests to lock the contract.
- Blocking-confirmation pattern (engine side): the engine's sync handler can block on a confirmation channel (registered in `session.PendingConfirms` keyed by event_id) because `routeIncomingEvent` dispatches sync events on separate goroutines — same mechanism as `sendSyncDataWithConfirmation`. On REJECTED/timeout, send `SYNC_REJECT` + return error so `handleSyncStart` fails the session; never transmit data before approval.
- Initial-sync detection: the first sync on a fresh install is identifiable by `ConnectionStateManager.getLastSync(source) === 0` at estimate time — the watermark is only persisted at SYNC_FINALIZE, and `initiateSync` escalates to `force_full_sync` exactly when no watermark exists. This cleanly separates "initial sync" (always prompt) from an explicit user "Force Full Re-Sync" (watermark exists → threshold applies).
- Audio Playback Race Condition (Bug Fix): The `preloadAudio` useEffect in ChatBubble called `AudioPlayer.loadAudioForMessage()` on mount for every audio bubble simultaneously. Since AudioPlayer is a singleton wrapping a single TrackPlayer queue, concurrent `reset()` + `add()` calls from all bubbles corrupted each other's state – the last bubble's audio data was in TrackPlayer but `currentMessageId` might point to a different message. Fix: removed `preloadAudio` useEffect entirely; audio is only loaded when the user explicitly taps play.
- Audio Duration Enrichment Pattern: Duration is parsed from raw base64 audio using music-metadata (no TrackPlayer interaction). For new messages: EntitySessionService parses and persists immediately after creation. For historical messages: ChatDetailScreen runs a best-effort Promise.all enrichment on load; failures are silently swallowed (bubble shows '--:--').
- Static vs Instance in AudioPlayer: `export default new AudioPlayer()` is an instance. To call static methods, import the class separately: `import AudioPlayer, { AudioPlayer as AudioPlayerClass } from './AudioPlayer'` then call `AudioPlayerClass.getDurationFromBase64(...)`.
- Native Module Integration: react-native-audio-record and react-native-track-player require careful iOS/Android configuration.
- Runtime Permissions Critical: Always check/request permissions before accessing mic/storage; provide clear error recovery UI.
- Audio Recording Flow: Initialize → Check permissions → Start → Stop → Read file → Convert to base64 → Store as TEXT → Cleanup temp file.
- Audio UI Pattern: Tap-to-start/stop works better than hold-to-record on mobile (less accidental recordings).
- Base64 Storage: Audio/image data stored as base64 TEXT columns (not BLOB), simplifies handling and transmission.
- Database Migrations: Forward-only SQL migration files in /src/database/migrations/, numbered sequentially, applied automatically on app startup.
- React Native Track Player v5 Migration:
   - Android build fixed by upgrading to v5.0.0-alpha0 (Kotlin type safety fixes for React Native 0.83.1)
   - API changes: `getPlaybackState()` returns `PlaybackState` object (contains `state` property), use `getProgress()` instead of separate `getPosition()`/`getDuration()` calls
   - Update options: `notificationCapabilities` replaces `compactCapabilities`
   - State enum values are now strings (e.g., State.Playing = "playing")
- Base64 Storage Pattern: Audio/image data stored as base64 TEXT in SQLite for simpler handling and cross-platform compatibility.
- Chat UI Patterns: KeyboardAvoidingView essential for iOS, FlatList with inverted prop for message lists.
- Image Picker: react-native-image-picker provides good UX with caption support.
- Pulse Animation: Animated.loop with scale transform creates effective recording indicator.
- Typing Indicator: Three-dot staggered animation (150ms delay between dots) provides familiar UX.
- Metro bundler hot reload excellent with TypeScript.
- React Native Paper dark theme integration smooth.
- Migration from react-native-document-picker to @react-native-documents/picker fixed RN 0.83.1 compatibility.
- Gradle 8.13 required (NOT 9.0+) for RN 0.83.1.
- Database encryption with SQLCipher + React Native Keychain provides secure storage.
- In-app test runner provides better developer experience than Node-based tests.
- Base64 TEXT storage simplifies binary data handling compared to BLOB columns.
- __DEV__ checks ensure development tools invisible in production builds.
- ADB PATH setup critical for newcomers.
- Good foundation prevents technical debt.
- EventEmitter3 (~2KB) provides type-safe, reliable event handling for React Native (Node.js EventEmitter unreliable).
- TypeScript generics for EventEmitter interfaces enable compile-time event validation.
- Comprehensive logging with [ServiceName] prefix critical for debugging async WebSocket flows.
- Android emulator uses 10.0.2.2 to access host's localhost (not 127.0.0.1).
- Harmony Link must bind to 0.0.0.0 (not localhost) for emulator connectivity.
- HANDSHAKE_PENDING event handler essential for user feedback during device approval.
- ConnectionManager pattern enables multiple concurrent connections with independent lifecycles.
- Decoupling transport layer from service logic improves testability and scalability.
- Context separation (SyncConnectionContext vs EntitySessionContext) provides clear responsibility boundaries.
- React useRef critical for avoiding stale closures in setTimeout/async callbacks (reconnect counter).
- Reconnect race condition fix: Only schedule reconnect from disconnected event if NOT in isConnecting state.
- Event listener accumulation: Always call removeAllListeners() before disconnecting to prevent duplicates.
- Auto-reconnect strategy: Always retry regardless of error type, exponential backoff [1s, 2s, 4s, 8s, 16s, 30s].
- Logging transport enhancement: Custom transport to stringify Error objects (including stacks) prevents '[object Object]' logs.
- Dual Entity Sessions: Mirror Harmony Link's EntitySessionManager - each chat needs user entity session + partner entity session for bidirectional events.
- Device Pairing Sequence: Handshake happens FIRST over unencrypted ws://, THEN security mode is selected after cert validation on wss://.
- Connection Types: 'sync' for device sync, 'entity-{entityId}' for chat sessions - distinct connection lifecycle and event routing.
- Preset Sync Pattern: App does NOT use Harmony Link management API — all preset data flows through WebSocket sync pipeline. Preset name is plain text input, extra params edited as key-value pairs locally.
- Schema-Driven Config: App uses same providerFieldSchemas.ts as Harmony Link web frontend — ensures consistent provider configuration experience across platforms.
- Module-Specific Schemas Dead Code: UI looks up `PROVIDER_SCHEMAS[providerType]` by base provider ID — module-specific schema keys (e.g., `tts_openai`) were dead code. Module-specific fields merged into main provider schemas with tooltips.
- isProviderConfigInUse Pattern: Single UNION ALL across all module config tables is more reliable and maintainable than 8 separate existence checks.
- providers.ts Garble Prevention: When regenerating a file with many similar CRUD functions, verify all function signatures and table names match — agent can garble large files with repetitive patterns. 15 provider schemas, no module-specific schemas (one schema per provider across all modules).
- chat_template_kwargs Dropped: Both Go backend and app dropped this dead column in migration 021 — no longer needed for template customization.
- Model Field Unification: Migration 000030 consolidates `embedding_model`/`image_model`/`imagination_model` into single `model` column per provider config table. RAG structs keep `EmbeddingModel` domain name, mapper bridges.
- Generic Provider Mappers: One extract/load pair per provider in Go backend, not per module. Module-specific mapper variants deleted as dead code.
- Shared isProviderConfigInUse: Single UNION ALL query replacing 8 separate existence checks for provider config deletion safety.
- InteractionSession Migration: Refactored from DualEntitySession (keyed by partnerEntityId) to InteractionSession (keyed by interactionId). Events are now interaction-scoped, not entity-pair-scoped. Supports N participants per session.
- JOIN Query Performance: JOIN-based queries for chat list (conversation_messages JOIN interactions) reduced N+1 queries to single round-trip for last message preview.
- Session Resume Flow: INIT_ENTITY response now includes `resumed: true` flag; service checks if backend had a suspended session and reconciles state without re-creating connection.
- Reply Mode Implementation: ChatDetailScreen reads replyMode from active session; EntitySessionService sets it during INIT_ENTITY as `reply_mode` in session capabilities.
- Emoji Action Resolution: EmojiService scans outgoing message text for emoji characters, resolves against entity's emoji_actions table, produces AdditionalEffects payload sent with SEND_MESSAGE event.
- Reply Mode Default: Defaults to "realistic" if no preference stored; toggle persists per chat partner in AsyncStorage.
- Managed credential UX: never expose an injected server credential in a normal text field — render as locked read-only password (editable=false + secureTextEntry + no eye + contextMenuHidden) so it can't be copied/revealed/overwritten. `v4.local.` prefix is the reliable engine-side discriminator between PASETO-mode and plain API-key mode.
- DB-update-only ≠ engine-updated: a DB write alone is invisible to the engine until a sync happens — bump `updated_at` on bulk token refresh AND call `SyncService.initiateSync()` so the change propagates immediately; `initiateSync()` is safe to call unconditionally (self-guards in-progress/disconnected), and the connect-time auto-sync remains the safety net.
- Live-catalog-is-truth: hardcoded model ids (fallbacks/defaults/placeholders) must come from the real `GET /v1/models` catalog — fake ids like `soulchat-v1` were never shipped by the backend. Guard test (fallback ids ⊆ live catalog) prevents silent regression.
- PowerShell quirk: jest `--testPathPatterns` with `|` gets mangled by PowerShell piping — use `--runTestsByPath "a" "b"` instead.
- Cert-trust lesson: self-signed engine cert regenerated per boot with SANs only localhost/127.0.0.1/::1 will ALWAYS fail TLS verification against a LAN IP — re-pair in Insecure-SSL mode; full pinning/trust was deliberately not engineered.
- Sync-race lesson: auto-sync must NEVER fire on a provisional/transient transport. The pairing flow connects plaintext ws:// → handshake → wss:// upgrade; syncing on the provisional connection means the subsequent upgrade tears the connection down mid-session, discarding buffered SYNC_DATA and orphaning `SyncService.currentSession` as 'in_progress' forever (the "Sync already in progress, skipping" stuck-state bug). Gate auto-sync on transport settlement; add an abort-on-connection-loss hook as defense-in-depth so the state machine can always return to a clean slate.
- WS-teardown-suppresses-events lesson: `ConnectionManager.disconnectConnection` calls `removeAllListeners()` BEFORE `disconnect()`, so the underlying WS 'disconnected' event is never emitted on deliberate teardown — any mid-session cleanup must be triggered by a dedicated signal (e.g. `sync:connection_replaced`) emitted BEFORE the teardown, or it will silently drop.
- lastSync-lives-in-AsyncStorage lesson: wiping the SQLite DB does NOT reset the sync watermark (stored in AsyncStorage per-source). A wipe without watermark reset makes the next sync incremental against a recent timestamp — the engine has nothing newer to send, so nothing transfers ("no data during sync"). Any full-DB-wipe flow must also clear the per-source `last_sync_timestamp:*` keys.
- Incremental-sync-is-truth lesson: 0 records transferred during a clean sync is NOT necessarily a bug — if the client reports a recent `last_sync_timestamp` and the engine has no records updated after it, the engine correctly returns nothing. Diagnose by reading both sides: device logcat (SYNC_REQUEST payload `last_sync_timestamp`) + engine logs (`GetChanged* called with sinceTimestamp`).
@Implementation[Sync]:
- Multi-Connection Architecture: ConnectionManager manages multiple WebSocket connections (sync + entity sessions).
- Services[4]: ConnectionManager (transport pool), SyncService (protocol logic), EntitySessionService (session management), ConnectionStateManager (JWT lifecycle).
- Contexts[3]: SyncConnectionContext (sync connection state/actions), EntitySessionContext (interaction session management), EmojiContext (emoji style preferences).
- EventEmitter3: Type-safe pub/sub pattern, connection-specific event routing (event:sync, event:entity).
- Protocol: HANDSHAKE_REQUEST → PENDING → ACCEPT → WSS upgrade → SYNC_REQUEST → DATA exchange → COMPLETE → FINALIZE.
- Entity Sessions: Session resume on disconnect, dangling event reconciliation.
- Screens[2]: ConnectionSetupScreen (IP/port input, handshake), SyncSettingsScreen (status, manual sync, force full sync).
- Database: sync_devices, sync_history, chat_messages, interactions, emoji_actions tables. Migrations in /src/database/migrations/ folder.
- Error Handling: Connection validation, timeout handling (30s), confirmation tracking, comprehensive logging.
@Implementation[Chat]:
- Database: chat_messages table with TEXT columns for base64-encoded audio data. Migrations in /src/database/migrations/.
- Repository: chat_messages.ts with full CRUD, bidirectional queries, pagination, async transcription updates. JOIN-based interaction queries.
- Audio Services: AudioRecorder.ts (tap-based recording with permission checks), AudioPlayer.ts (playback controls).
- Voice Features: STT (Speech-to-Text) and TTS (Text-to-Speech) integrated and working.
- Native Modules: react-native-audio-record, react-native-track-player, react-native-fs, react-native-image-picker.
- Platform Setup: iOS (NSMicrophoneUsageDescription), Android (RECORD_AUDIO, READ/WRITE_EXTERNAL_STORAGE), runtime permission checks.
- Event Constants: TYPING_INDICATOR, RECORDING_INDICATOR, CONNECTION_PING, CONNECTION_PONG, STT_INPUT_AUDIO, STT_OUTPUT_TEXT.
- EntitySessionService: InteractionSession management (replaces DualSession), message sending (text/audio), STT async flow, heartbeat handling, reply mode support, session resume.
- UI Screens: ChatListScreen (entity selection, conversation list via JOIN), ChatDetailScreen (messaging with QoL: last message actions, new messages divider, auto-scroll, reply mode toggle).
- UI Components: ChatBubble (context menu for last message: delete/edit/regenerate, emoji-rich text), ChatInput (tap-based recording, permission recovery, emoji autocomplete), TypingIndicator, NewMessagesDivider.
- Navigation: ChatDetail route with impersonatedEntityId (roleplay support) and partnerEntityId/partnerCharacterId params.
- QoL Features: Last message actions, new messages tracking via ChatPreferencesService, mark-as-read on scroll, async transcription editing, reply mode toggle.
- Image Messages: Not fully implemented yet (placeholder support exists).
- Documentation: Complete integration guide in /docs/HARMONY-LINK-INTEGRATION.md with 5 detailed diagram files.
@Implementation[Interactions]:
- DB Migrations: 000024-000028 covering interactions table, drop session_id, interaction summary, memory lifecycle fields, presence_type.
- Model: Interaction interface (matches Harmony Link Go struct) with id, entity_id, scope, participant_key, participant_ids, status, presence_type, summary, memory_id, continued_interaction_id.
- Repository: interactions.ts with full CRUD (getInteractionById, createInteraction, updateInteraction), query by participant key, entity-scoped active interactions, getRecentPhoneInteractions, entityHasPhoneInteraction, getLastInteractionMessage (JOIN-based with TEXT column loading).
- Derivation Functions: deriveScopeFromParticipants (world/private/group), deriveParticipantKey (sorted pair/group join/empty for world).
- Sync Integration: SyncService syncs interactions via WebSocket pipeline; conversation_messages refactored to JOIN-based queries for chat list preview.
- Session Refactor: InteractionSession replaces DualEntitySession; events keyed by interactionId; supports N participants per session; navigation adapted.
@Implementation[Emoji]:
- Emoji Types: Ekman8Emotion (joy, sadness, trust, disgust, fear, anger, surprise, anticipation), EmotionEffect (emotion + signed delta), MetabolismVector, EmojiAction, AdditionalEffects, ResolvedMessageActions.
- Emoji Picker Components: EmojiPickerModal (full-screen with category tabs), EmojiPickerInline (compact for chat), EmojiGrid (virtualized grid), EmojiItem (single emoji with skin tone), EmojiSearchBar (keyword search), CategoryTabBar (swipeable category tabs), SkinToneSelector (5 skin tone modifiers), EmojiAutocomplete (inline text replacement).
- Emoji Display: EmojiAwareText (intelligent emoji/text segmentation), EmojiText (renders emoji in chat bubbles), ChatBubble custom rendering for emoji-rich messages.
- Emoji Services: EmojiService (data loading, search, skin tone variants), EmojiContext (style preferences, provider state management), EntityEmojiActionService (action CRUD + message resolution).
- Emoji Actions: auto-resolved on send via EntityEmojiActionService (substitution + emotion effects); the per-entity editor UI (EmojiActionEditorScreen / EmojiActionCard / EmojiActionEditModal) was removed 2026-08-14.
- DB Migrations: 000022 (entity_emoji_actions table with emoji, action_type, emotion_effects JSON, style/bubble_color, cooldown_seconds), 000023 (deleted_at for soft deletes).
- Sync: emoji_actions synced via WebSocket pipeline, AdditionalEffects payload sent with SEND_MESSAGE events.
@Implementation[SamplingPresets]:
- App does NOT call Harmony Link management API for presets — only syncs via WebSocket
- Preset name is plain text input (user types name that exists on server)
- Extra params edited locally as key-value pairs (typical_p, tfs, dry_*, etc.)
- Precedence: explicit config > extraParams > preset values > library defaults
- DB: migration 021 added sampling_preset_name + extra_params TEXT columns to module config tables
- Provider config auto-created/updated during module config save via saveProviderConfig()
- Advanced Sampling Parameters component for OpenAI-family providers (openai, openaicompatible, openrouter)
- Provider field schemas in app mirror Harmony Link frontend (providerFieldSchemas.ts, moduleConfiguration.ts, moduleDefaults.ts)
- chat_template_kwargs column dropped from both Go backend and app (migration 021)
